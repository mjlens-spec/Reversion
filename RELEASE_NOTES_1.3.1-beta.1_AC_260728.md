# Reversion 1.3.1-beta.1（内部测试版，不公开发布）

针对 1.3.0 发布后验收发现的三处问题的修复版。仅本地分发试用，不上 GitHub Release、不进入自动更新链。

## 修复

1. **编辑器右侧空白区**：两套内置主题误把行宽 `max-width` 同时压在滚动视口（`.mu-editor`）上，宽窗口下右侧出现整片死区、滚动条卡在窗口中间。现改为只约束内容列（`.mu-container`），视口占满窗口、内容列居中。
2. **全新安装误弹「更新说明」**：首次启动（或全新配置目录）会弹出本不该出现的更新完成弹窗，并遮挡编辑区。现主进程在偏好迁移前捕获「上一次运行版本」，全新安装静默记录版本；从旧版本升级仍正常弹一次。
3. **深色模式下默认主题失效**（1.2.0 起即存在，macOS 晚间自动切深色后由 e2e 暴露）：两处覆盖会在系统深色外观时压掉 lens-design——① 首启种子被 `nativeTheme` 判定覆盖为 `dark`；② 种子 `followSystemTheme: true` 使每次启动都执行「跟随系统切主题」。现两处都改为尊重 lens-design 默认；「跟随系统外观」仍可在偏好设置中手动开启（浅色→lens-design / 深色→dark 的映射保留）。**此项为产品行为判断（按 1.2.0「默认主题 = lens-design」决策执行），如需保留跟随系统为默认请指出。**
4. **选中文字弹出的格式工具条重设计**：10px 圆角 + 发丝描边、按钮间距与圆角悬停态、active 态按主题色出淡色底（Lens Design 为孔雀蓝，Claude-like 为陶土橙）、清除格式项以短分隔线独立成组。所有配色走主题变量，各主题自动适配。

## 工程

- 清零 1.3.0 遗留的 7 个 ESLint error；修复 pdf 单测在全量并行下的偶发失败（复现于未修改基线）。
- 门禁：desktop 单测 777/777（连续两轮）、typecheck 通过、lint 0 error、muya stylelint 通过。

## 兼容性

与 1.3.0 相同：Apple Silicon（arm64）、ad-hoc 签名未公证、用户数据目录不变，可直接覆盖安装 1.3.0，也可回退。
