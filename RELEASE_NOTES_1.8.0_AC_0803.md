# Reversion 1.8.0

本次版本把左侧「文件」从已打开文档列表升级为可浏览的文件夹树，让单文件写作也能直接查看和打开相邻文档。

## 新功能

**打开文档时显示所在目录。** 即使没有主动打开项目文件夹，侧栏也会以当前文档所在文件夹为根，呈现同级文件与子文件夹。可继续向上浏览父目录，也可逐层展开子文件夹。

**文件状态清楚区分。** 支持打开的 Markdown 文档保持正常颜色；图片、二进制文件等不支持的格式以暗灰色显示并禁用；当前文档持续显示选中高亮。

**项目文件夹行为保持不变。** 主动打开项目目录时，侧栏继续使用原有项目树及其新建、重命名、删除等操作；单文件上下文树保持只读，避免修改后与编辑器状态不同步。

## 改进

**大型目录按需加载。** 目录条目通过单次批量请求读取，同一目录内切换文档会复用已经加载的树；子目录只在展开时读取，普通文件不会在主进程中逐项同步探测磁盘。

**文件系统边界更加稳健。** Windows 盘符根目录、UNC 共享路径、符号链接、断链、无权读取的目录和失败重试均有明确处理。符号链接是否可打开以真实目标文件为准。

**键盘与无障碍支持补齐。** 文件与文件夹可通过 Enter、空格键和方向键操作；当前项、展开状态与禁用状态提供对应语义。新增提示与错误文案已覆盖 10 种界面语言。

## 验收

- Desktop 完整单元测试：837/837
- 文件树定向测试：22/22
- Electron 源码交互回归：5/5
- 正式打包应用 E2E：20/20
- 工具仓契约测试：98/98
- TypeScript、ESLint、production build：通过
- DMG 与更新 ZIP 的 SHA-256、ZIP 完整性、DMG 校验、`latest-mac.yml` 的版本/大小/SHA-512：通过
- 正式应用版本、arm64 架构与深度签名：通过
- Sol 5.6 X-High 最终工程审查：APPROVED

## 兼容性

- Apple Silicon（arm64）
- macOS 应用路径：`/Applications/Reversion.app`
- 用户数据目录继续使用 `~/Library/Application Support/marktext`
- Bundle ID 继续使用 `com.github.marktext.marktext`
- 反文采用稳定 ad-hoc 签名，未经 Apple 公证
