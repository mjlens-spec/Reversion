# Reversion 1.3.2

本次更新新增应用内导入 Typora 主题，并对编辑器界面做了一轮打磨。

## 导入 Typora 主题

「主题」菜单新增「导入主题（Typora 兼容）」。选择一个 Typora 主题的 `.css` 文件后，反文会就地完成转译：生成编辑器主题与 HTML/PDF 导出主题，写入用户主题目录，在菜单的「已导入主题」分组中列出并立即启用。

导入完成后会显示本次转换了多少条选择器、丢弃了多少条 Typora 专有规则，可直接打开兼容报告查看逐条明细。Typora 主题对其自身界面结构的依赖程度不同，还原度以报告为准。

此前该能力只存在于命令行脚本中，应用内不可用。

## 编辑器界面

- 选中文字后弹出的格式工具条改用 Material Symbols 图标，并在鼠标悬停时显示按钮功能与快捷键（加粗 ⌘+B 等）。此前十个按钮只有图标，且图标为位图缩放，高分屏下发虚。
- 左侧竖排的文件、搜索、目录按钮改用同一套图标，当前面板以圆角底色块标示，新增悬停反馈与文字提示。
- 右上角字数统计不再是单个混合数字与单字母标识：现固定显示「总字数」，并将中文字数与英文单词数分开呈现。悬停可查看字数、字符数、段落数的完整明细。

## 修复

- 转译主题的标题样式此前不生效：转译器仍按旧编辑器内核的 DOM 生成标题选择器，与当前内核不匹配，导致 h1–h6 规则全部落空。

## 兼容性

- Apple Silicon（arm64）
- macOS 应用路径：`/Applications/Reversion.app`
- 用户数据目录继续使用 `~/Library/Application Support/marktext`
- Bundle ID 继续使用 `com.github.marktext.marktext`
- 可从 1.2.0 / 1.3.x 通过应用内更新直接升级

反文采用稳定 ad-hoc 签名，未经 Apple 公证。首次打开如被 Gatekeeper 阻止，请在访达中按住 Control 点击应用，再选择「打开」。
