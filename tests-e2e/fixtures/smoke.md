# Reversion E2E Smoke Fixture

## Section One

This fixture exists to give the Reversion e2e smoke suite something
real to render: headings, lists, a table, inline code, and a formula.

### A List

- first item
- second item
  - nested item
- third item

1. step one
2. step two
3. step three

### A Table

| Feature | Status | Owner |
| --- | --- | --- |
| TOC default | done | E1 |
| Theme registration | done | E1 |
| Inline live render | done | E1 |

### Inline Code and a Formula

Use `const x = 1` for a constant, and the identity $E = mc^2$ links
energy and mass. A fenced block follows:

```js
function reversion() {
  return 'lens-design'
}
```

> A blockquote, for good measure.
