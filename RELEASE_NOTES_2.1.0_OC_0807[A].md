# Reversion 2.1.0

2.1 将中文排印、阅读参数、沉浸写作、界面密度与明暗外观合并为一次正式升级，并采用 Lens 确认的手持铅笔雕版图标。文档、设置、历史记录、Bundle ID 与自动更新链保持兼容。

## 中文排印

- 中西文间距和标点挤压只作用于渲染层，不修改 Markdown 源文件。
- 中文语境下输入直引号会生成成对弯引号；代码、公式与链接 URL 等保护区不触发。
- 「整理中文排印」提供四项独立规则、替换计数、保护区与单步撤销。

## 阅读与沉浸写作

- 正文字号、正文栏宽、行高与段距可独立调节，编辑器和样式化导出共用同一组值。
- 表格字号随正文联动，始终比正文小 1 px；宽表格继续横向滚动。
- 专注模式淡出非活动块。打字机模式默认只在输入时居中，按窗口高度计算中线，并为文首文末提供不进入导出的虚拟留白。
- IME 组合期间不回拉；源码模式下两种模式暂停，退出后恢复。

## 界面与外观

- 正文宽度、默认 16 px 字号、约 19% 侧栏、12–13 px 侧栏文字、三段式顶部切换与 6–7 px 圆角滚动条，统一参考 Claude Code 的信息密度。
- 「目录」工作区更名为「大纲」，侧栏条目压缩；语义缩略图加宽至 32 px，在宽窗口中悬停 150 ms 后显示对应位置的真实渲染内容。
- Lens Design 与 Claude-like 支持浅色、深色和跟随系统。Typora 导入主题激活时界面锁定浅色，HTML、PDF 与 PNG 导出恒为浅色。

## 2.1.0 默认 App Icon

- 受控源资产为 `icon/reversion-hand-pencil-engraving_OC_0807B.png`：1024×1024、sRGB、近白暖纸底、深褐色手持铅笔雕版线稿。
- Electron 配置、ICNS、应用包静态资源、Finder、Dock 选择器与 DMG 统一使用此稿；未选择默认图标的既有用户迁移到新稿，主动选过其他方案的用户不变。
- 其他 Icon 草稿保留，编辑器内部功能图标与品牌 Logo 不随本项替换。

## 验证

- 工具仓契约测试：101 项通过。
- 桌面应用单元测试：895 项通过。
- Muya 编辑器测试：1483 项通过。
- TypeScript / Vue 类型检查与 Electron 生产构建通过。
- 正式 ZIP、DMG 的 SHA-256、版本号、arm64 应用包、深度签名、稳定指定要求、Quick Look 扩展与更新清单均已回读。
- 最终 `.app/Contents/Resources/icon.icns`、静态 PNG 与 Dock 选择器资源逐字节回读；DMG 在 Finder 中放大显示新图标。

## 兼容性与发布边界

- 本次产物仍为 Apple Silicon `arm64`，使用稳定应用标识的 ad-hoc 签名，未经 Apple 公证。
- 本地候选产物已生成；GitHub 发布与本机在线更新仍须通过 Lens 的 C6 明确批准。
