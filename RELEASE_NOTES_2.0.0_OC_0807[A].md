# Reversion 2.0.0

2.0 将「反文」的页面形态、长文档导航、字体系统和命令入口合并为一次正式升级。旧版本的文档、设置、历史记录与自动更新链保持不变。

## 页面新形态

- 侧栏改为很浅的浮层，使用 16 px 圆角、轻微透明、背景模糊与克制阴影，与正文空间分开。
- Mermaid 图示拥有独立视觉容器，图形自动居中，宽图可滚动，不会裁切。
- highlight 改为平面色块，支持绿、蓝、橙、粉四色；原有渐变马克笔效果已移除。
- 引用块使用浅色表面、充足留白与 4 px 单边强调线，编辑区与导出主题保持一致。

## 语义缩略图

- 编辑区右缘新增 16 px 文档概览，以不同视觉密度区分标题、段落、代码、引用、表格、列表、分隔线和图示。
- 视口框随编辑器滚动，点击可跳转，按住拖动可连续浏览。
- 通过「视图 → 语义缩略图」开关并持久化。窗口小于 900 px、进入源码模式或打开模态弹窗时，缩略图自动隐藏或停止命中。

## 双字体阅读系统

- 设置中新增「西文正文字体」与「中日韩字体」两个选择器，均可搜索系统字体并查看实时预览。
- 默认继续跟随主题，所以升级后不会改变现有文档的排印。
- CJK 选择器会显示当前实际使用的字体。字体栈在编辑器与 HTML、PNG、PDF、打印导出中共用；DOCX 仍由 Word 自身管理排版。

## 双语命令面板

- `Command+Shift+P` 现在同时检索命令、最近文件和当前文件夹。
- 命令保留中英文双语名称，无论界面使用哪种语言，中英文关键词都能命中。
- 最近执行的 5 条命令优先显示。必须依赖文档的命令在无文档时保留可见但置灰。
- 新面板支持上下键、Enter、Esc 和 IME 组合输入保护。`Command+K` 继续用于目录，本版本不做迁移。

## 验证

- 应用仓单元测试：871 项通过。
- 2.0 专项与命令面板端到端：7 项通过。
- 正式打包应用 E2E：20 项通过。
- Muya 2 中文输入法矩阵：72 项通过。
- 工具仓契约测试：99 项通过。
- 10,000 行长文档性能门禁：冷启动 1.661 s、渲染器就绪 0.960 s、输入 P95 36.2 ms，滚动 62 帧。
- TypeScript 类型检查、Electron 生产构建与视觉对照通过。
- DMG 与更新 ZIP 的 SHA-256、ZIP 完整性、DMG 校验，以及 `latest-mac.yml` 的版本、大小和 SHA-512 均已核验。
- 从正式更新 ZIP 解出的应用版本、arm64 架构、指定要求与深度签名均已核验。

## 兼容性

- 本次发布仍为 Apple Silicon `arm64`。
- 应用路径、用户数据目录与 Bundle ID 不变。
- 应用使用稳定应用标识的 ad-hoc 签名，未经 Apple 公证。
