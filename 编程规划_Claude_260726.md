# Reversion 反文 · 编程执行规划 v1

日期：2026-07-26
上位文档：[开发规划_Claude_260726.md](开发规划_Claude_260726.md)
本轮范围：M0–M4（源码化、内核品质、行内渲染、表格、主题兼容），重组为 5 个中等工作量里程碑 E1–E5。M5（导出）、M6（中文排印）留待下一轮。

## 一、角色分工与协作协议

| 角色 | 模型 | 职责 |
| --- | --- | --- |
| 规划 / Advisor / 审核 | Fable 5 | 里程碑规格与任务简报起草、行为规格评审、中期检查、代码审查（diff 级）、风险决策、验收终审 |
| 执行（内核级） | Opus 5（high） | 内核改动：IME、光标与选区、行内渲染行为、表格交互；按简报交付 diff + 测试证据 |
| 执行（机械性） | Sonnet 5 | 补丁迁移、测试脚手架与用例编写、批量主题转译验证、基准文档生成器、文档整理 |

任务表中标注 **[S]** 的任务默认派给 Sonnet 5；未标注的默认 Opus 5（high）。Sonnet 的周上限与全模型周上限分开计算，下放机械性工作既省额度、又扩大每周总吞吐。

**协作方式（二选一或混用）**

1. 主会话保持 Fable 5，由 Fable 通过子代理派发任务给 Opus 5 执行（本环境支持按任务指定模型），Fable 在同一会话内审查产出。
2. 你在独立会话 `/model claude-opus-5` 执行任务简报，把 diff / PR 拿回 Fable 会话终审。

**每个任务的定义完成（DoD）**：简报内验收项全部通过 + 相关测试绿 + diff 经 Fable 审查无阻塞意见。

**节奏**：每个里程碑四步——规格评审（Fable）→ 执行（Opus，3–6 个任务）→ 中期检查（Fable，里程碑过半时）→ 终审 + 回归（Fable）。

## 二、额度预算

**口径与假设（需 E1 实测校准）**

- 官方仅公布相对量级：5 小时滚动窗口（2026-05 起翻倍）+ 全模型周上限 + Sonnet 单独周上限；具体 token 配额未公开且动态调整。
- 工作假设：**1 个 Max 5X 的 5 小时窗口 ≈ 2.5–4 小时 Opus 5（high）连续高强度 agentic coding**（high 推理档思考 token 开销大，取偏保守值 3h 估算）。
- 参考量级：高强度执行每活跃小时处理约 2–4M token（含缓存读，缓存读占大头），纯输出约 3–8 万 token/小时。
- **校准机制**：E1 第一个窗口结束时记录实际消耗（活跃时长、任务完成量），修正后续所有预算。以下数字的置信度：E1/E2 较高，E3 最低（IME 修复深度未知）。

**预算表**（2026-07-26 修订：Sonnet 分工已采纳；E1 任务 1 已完成，上游 fork 点钉死；E3 因上游已有 IME 修复分支下调）

| 里程碑 | 内容 | Opus 窗口数 | Sonnet 窗口数（独立周上限） | 处理 token 量级 |
| --- | --- | --- | --- | --- |
| E1 | 源码构建链路 | **2–4 个** | 1–2 个 | 15–50M |
| E2 | Typora 主题兼容层 MVP | **2–3 个** | 1–2 个 | 16–50M |
| E3 | IME 专项 + 长文档性能 | **4–10 个** | 2–3 个 | 35–120M |
| E4 | 行内实时渲染 2.0 | **4–6 个** | 1–2 个 | 25–80M |
| E5 | 表格编辑 | **4–7 个** | 1–2 个 | 30–100M |
| 合计（本轮） | | **16–30 个** | 6–11 个 | 120–400M |
| 下一轮参考 | M5 导出 / M6 中文排印 | 2–4 / 3–5 个 | 各 1–2 个 | — |

**日历折算**：窗口本身每 5 小时滚动刷新，真正的约束是周上限。Opus 部分走全模型周上限，Sonnet 部分走独立周上限，估计全速推进 **3–5 周**耗完本轮额度（动态限额，以 E1 实测为准）。

**实测校准（2026-07-26，E1 收官时）**：E1 全部 5 个任务 + E2 前 2 个任务实测消耗——Opus 三任务约 66 万 subagent token、Sonnet 三任务（含一次修订轮）约 115 万，合计约 180 万，另加主会话（Fable）评审编排开销；六任务 agent 墙钟合计约 2.2 小时。**远低于原预算下限**：规格驱动 + 定向简报的子代理模式，效率显著高于按「连续交互式编码」假设的估算。据此 E3–E5 预算下调约 40–50%：E3 → 2–5 个 Opus 窗口、E4 → 2–3 个、E5 → 2–4 个。保留一条不确定性：E3 的 IME 修复深度仍未知，任务 2 分级清单出来前该项不锁死。

## 三、里程碑详细分解

### E1 · 源码构建链路（对应 M0）｜2–4 窗口

**目标**：结束「补丁二进制」模式，源码一条命令直出 DMG，为 E3–E5 的内核改动铺路。

**任务 1 已完成（2026-07-26，Fable 核实）**：上游为 `marktext/marktext`（仍在活跃维护，默认分支 `develop` = 0.20.0-dev，最近推送 2026-07-05）。fork 点 = tag **`v0.19.1`**（commit `d218718c`，2026-06-06 发布，为最新稳定版），Electron ~42.1.0 与现装构建一致。pnpm monorepo：`packages/desktop` + `packages/muya`（muya v2 TS，自带 Playwright e2e 套件）。源码已克隆至 `upstream/marktext`，本地分支 `reversion/1.1-base` 指向 v0.19.1，`upstream/` 已加入 .gitignore。

| # | 任务 | 产出 |
| --- | --- | --- |
| 1 | ~~上游考古~~ **已完成**，见上 | fork 点：`v0.19.1` |
| 2 | 本地构建跑通：pnpm 安装 → 构建 → app.asar，与现装 asar 做结构 diff | 差异清单 |
| 3 | **[S]** 补丁移植：品牌/双语 InfoPlist/主题注入/默认 TOC/runtime CSS 全部转源码级；`tests/` 6 个契约测试迁移 | 源码级修改 + 测试绿 |
| 4 | 发布管线：`build-release.sh` 改源码直出 DMG/ZIP/latest-mac.yml；实测 1.1.0 → 新构建自动更新 | 可复现发布流程 |
| 5 | **[S]** e2e 脚手架：基于上游 `packages/muya/e2e` 的 Playwright 套件扩展应用级冒烟（启动、开文档、基础断言） | e2e 冒烟集 |

**Fable 审核点**：任务 2 的差异清单（确认可复现）；任务 4 的更新兼容方案；终审。
**验收**：一条命令源码出 DMG；契约测试 + e2e 冒烟全绿；旧版可自动更新到新构建。
**风险**（已收窄）：构建产物与官方 DMG 有差异属预期（签名、图标），关注点只在 asar 内 `out/` 结构等价；若依赖锁文件缺失导致装不出可用依赖树，用上游 CI 配置对齐 Node/pnpm 版本。

### B1 · 品牌一致性清理（2026-07-26 追加，用户提出）｜0.5–1 个 Sonnet 窗口，并入 1.2.0 发布范围

**目标**：清除源码构建版中所有面向用户的 MarkText 残留，统一为 Reversion 品牌。在 beta.2 之后、正式 1.2.0 之前完成。

| # | 任务 | 策略（已定） |
| --- | --- | --- |
| 1 | **[S]** 全量品牌审计：对 1.2.0 构建产物逐个界面/菜单/对话框排查 MarkText、luo han 等残留字符串（含九种语言的本地化文件），出清单 | 审计先行，逐项核销 |
| 2 | **[S]** About 对话框：版权行改为 Reversion 署名 + 「Based on MarkText」上游致谢行 | MIT 版权保留义务由 LICENSE/NOTICE.md 承担，About 层面署名 Reversion 合规 |
| 3 | **[S]** 帮助菜单：「支持 MarkText」条目**保留**（用户决策），标注为上游项目支持入口；其余 MarkText 链接按审计清单处置 | 保留上游支持入口 |
| 4 | **[S]** 更新日志：菜单项改为打开 `https://github.com/mjlens-spec/Reversion/releases`（用户决策） | 不再维护内置 changelog |
| 5 | **[S]** 菜单栏应用名：在真机复核 E1.3 的 `app.setName` 修改已生效（e2e 已有断言，需人工确认视觉呈现） | 大概率已修复，复核确认 |
| 6 | **[S]** e2e 扩展：品牌残留断言加入 tests-e2e 冒烟（关键界面无 "MarkText"/"luo han" 字样，白名单放行「支持 MarkText」条目） | 防回归 |

**Fable 审核点**：任务 1 审计清单（决定处置范围）；终审。
**验收**：审计清单全部核销或白名单化；e2e 品牌断言绿；1.2.0 发布检查清单更新。
**完成状态**：已随 1.2.0 发布（2026-07-26）。菜单栏显示名经 B2 的 productName 迁移解决。

### B3 · 更新体验 UI（2026-07-26 追加，用户提出）｜1–2 个 Opus 窗口 + 1 个 Sonnet 窗口，并入 1.3.0

**现状（已核实源码：`main/menu/actions/marktext.ts`、`renderer/src/store/autoUpdates.ts`）**：整条更新链只有四个瞬时 toast。三处硬缺口：

1. **下载期间零反馈**——`autoUpdater` 的 `download-progress` 事件**完全没有监听**。ZIP 约 133 MB，慢网下用户会以为应用卡死或更新失败。
2. **无更新说明**——`UpdateInfo.releaseNotes`（GitHub provider 会带上 Release 正文，即我们写的中文发布说明）从未被读取或展示，用户装完不知道改了什么。
3. **强退风险**——`update-downloaded` 后直接 `setImmediate(autoUpdater.quitAndInstall())`，不给保存机会。源码第 49 行的 TODO 已承认此问题。这是真实的数据丢失风险，且正好位于本次要改的代码路径上，一并修掉。

| # | 任务 | 内容 |
| --- | --- | --- |
| 1 | 主进程事件补齐 | 监听 `download-progress`，经 IPC 推送 `{percent, transferred, total, bytesPerSecond}`；`update-available` 一并透传 `version` 与 `releaseNotes`；`update-downloaded` 改为等用户确认再 `quitAndInstall`，确认前先走现有的未保存文档检查（移除 `setImmediate` 强退） |
| 2 | 下载进度 UI | 常驻式进度条（非瞬时 toast）：百分比 + 已传/总量 + 速率 + 预计剩余；可收起为角标、不阻塞编辑；失败 / 重试 / 取消三态明确。同步驱动 macOS Dock 进度条（`win.setProgressBar()`），窗口最小化时也看得到进展 |
| 3 | 更新完成弹窗 | 首次启动检测「版本已变化」→ 弹出本轮更新说明。取数双路：主用 GitHub Releases API 取当前版本的 Release 正文（**同时覆盖手动装 DMG 的场景**），备用为下载时持久化的 `releaseNotes`（离线可用）。Markdown 渲染复用应用自身能力；弹窗内提供「查看完整发布说明」外链。展示后记录 `lastSeenVersion`，不重复弹 |
| 4 | **[S]** 视觉与文案 | 进度条与弹窗按 Lens Design 体系（孔雀蓝进度、纸面底色、三字体槽位），深浅色模式各一套；文案九语言本地化 |
| 5 | **[S]** 测试 | 单元：进度事件节流与格式化（速率 / 剩余时间）、版本比较与 `lastSeenVersion` 状态机、releaseNotes 双路取数降级；e2e：伪造 `download-progress` 序列断言 UI 状态推进，伪造版本变化断言弹窗出现且仅出现一次 |

**设计前置**：任务 2/3 的具体视觉稿在动手前先列候选方案与 skill 给 Lens 确认（按设计类任务约定），不直接开工实现。

**Fable 审核点**：任务 1 的 IPC 契约与「确认后再退出」的保存流程衔接；任务 3 的取数降级策略；视觉稿评审；终审。
**验收**：真实更新链路（1.2.0 → 1.3.0）实测可见连续进度、可取消、退出前有保存机会、重启后弹出中文更新说明且仅弹一次。**这条真实链路彩排同时补上 B2 遗留的 V5 验证欠账。**

### E2 · Typora 主题兼容层 MVP（对应 M4）｜2–3 窗口 + Sonnet，可与 E1 并行

**目标**：CLI 转译器把 Typora 主题一键转为反文主题（编辑器 + 导出双目标），5 个流行主题验证分级。

| # | 任务 | 产出 |
| --- | --- | --- |
| 1 | 映射表提取：diff `.tmp/claude-source/claude-like.css` 与 `themes/claude-like-marktext.css`，形成 selector / 变量 / 剥离规则三张映射表 | 映射规格（Fable 评审） |
| 2 | 转译器 `scripts/import-typora-theme.mjs`（postcss）：`#write`→`.mu-container` 等 selector 重写、`--bg-color` 等变量映射、三字体槽位注入、Typora UI 选择器剥离 | 转译器 + 单测 |
| 3 | 双目标输出：编辑器主题 + 导出主题（`.markdown-body`），附兼容报告（丢弃/未映射规则清单） | 每主题三件产物 |
| 4 | **[S]** 验证集：GitHub / Night / Drake / Vue 四主题 + claude-like 回归（转译版 vs 人工移植版 e2e 截图对比） | A/B/C 分级清单 |
| 5 | **[S]** 文档：兼容说明、已验证主题目录、已知限制 | 用户文档 |

**Fable 审核点**：任务 1 映射规格；任务 4 分级结论；终审。
**验收**：5 主题中 ≥3 达 B 级（基本兼容）以上；claude-like 转译版与人工版视觉一致。

### E3 · 内核品质：IME + 长文档性能（对应 M1）｜4–10 窗口 + Sonnet，依赖 E1

**目标**：中文输入零已知复现 bug；10k 行文档输入延迟 P95 < 50ms。

**利好（2026-07-26 核实）**：上游正在活跃处理 IME 问题，已有进行中分支 `fix/muya-ime-codeblock-rerender-4851`、`fix/1250-ime-tab-composition` 等。策略从「全部自己修」调整为「先评估/合入上游修复，再补自己矩阵内的剩余项」，并把我们的修复以 PR 形式回馈上游（降低长期维护成本）。

| # | 任务 | 产出 |
| --- | --- | --- |
| 0 | 上游 IME 现状盘点：相关 issue（#4851、#1250 等）与修复分支逐个评估，可用的 cherry-pick 进 fork | 上游修复合入清单 |
| 1 | **[S]** IME 测试矩阵文档 + composition 事件 e2e 模拟器（拼音整句/候选态跨段/标点配对冲突/行内代码与公式边界/撤销后重输/双拼/五笔/日文假名） | 矩阵 + 模拟器 |
| 2 | 复现与分类：全矩阵跑一遍，bug 清单按「丢字/跳光标/重复字/渲染打断」分级 | 分级清单（Fable 参与定级，**预算修正点**） |
| 3 | 修复批次一：高频致命项 | diff + 矩阵回归 |
| 4 | 修复批次二：剩余项 + 撤销/粘贴保真顺带修复 | diff + 矩阵回归 |
| 5 | **[S]** 性能基准：10k 行混合文档生成器 + 指标采集（输入延迟 P95 / 冷开 / 滚动帧率） | 基准报告 |
| 6 | 性能优化：按测量结果择路——渲染节流 → 视口外惰性渲染 → 超阈值降级提示 | 达标或差距说明 |

**Fable 审核点**：任务 0 合入清单；任务 2 分级清单（同时修正 E3 剩余预算）；任务 5 基准报告（决定优化路径）；终审。
**验收**：矩阵零已知复现；性能达标，或给出明确剩余差距与后续方案。
**不确定性说明**：本里程碑预算区间最宽，任务 2 结束时必须重估一次。

### E4 · 行内实时渲染 2.0（对应 M2）｜4–6 窗口 + Sonnet，依赖 E1，建议在 E3 后

**目标**：从 CSS 层形态对齐进到行为层手感对齐。

| # | 任务 | 产出 |
| --- | --- | --- |
| 1 | **[S]** 行为规格表：对照 Typora 逐项记录目标行为（Fable 起草框架，Sonnet 实测补全 Typora 实际表现） | 规格表（Fable 评审后冻结） |
| 2 | 显隐粒度改造：active 从段落级收窄到光标所在行内语法范围 | diff + e2e |
| 3 | 布局稳定与光标映射：展开/收起时光标视觉锚定；点击落点、方向键跨元素 | diff + e2e |
| 4 | 链接体验：渲染态 Cmd+Click 打开；URL/文字分离编辑弹层 | diff + e2e |
| 5 | 行内代码/公式进出稳定性（进入显原文、退出即渲染、无闪烁）；图片选中态与拖拽调宽（末位，可顺延） | diff + e2e |

**Fable 审核点**：任务 1 规格冻结；任务 3 后中期检查（手感主观项由你试用打分）；终审。
**验收**：规格表全项 e2e 通过 + 你的试用确认。

### E5 · 表格编辑（对应 M3）｜4–7 窗口 + Sonnet，依赖 E1，与 E4 共享块级选区基建

**目标**：表格高频操作对齐 Typora。

| # | 任务 | 产出 |
| --- | --- | --- |
| 1 | **[S]** 交互规格：导航（Tab/Shift+Tab/Enter/方向键）、行列操作、重排、粘贴导入行为逐项定义 | 规格表（Fable 评审后冻结） |
| 2 | 单元格导航 + 悬浮工具条 + 右键菜单（前后插入行列/删除/列对齐三态） | diff + e2e |
| 3 | 重排：行/列拖拽手柄；Alt+↑/↓ 移动行并推广至任意段落/块级 | diff + e2e |
| 4 | 粘贴导入与导出：TSV/CSV/Excel 粘贴成表；表格复制为 Markdown/HTML | diff + e2e |
| 5 | 宽表格横向滚动容器，替换主题内 13px 压缩方案（同步更新两套主题与契约测试） | diff + 测试更新 |

**Fable 审核点**：任务 1 规格冻结；任务 3 后中期检查；终审。
**验收**：规格表全项 e2e 通过；宽表格新方案在两套主题下表现一致。

## 四、执行顺序

```
E1 ──┬──> E3 ──> E4 ──> E5
E2 ──┘（与 E1 并行，先出对外可见成果）
```

每个里程碑收尾时：更新预算表实测值 → 修正剩余里程碑估算 → Fable 出下一里程碑任务简报。
