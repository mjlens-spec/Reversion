# Reversion 2.1.1

2.1.1 是针对 2.1.0 的修复版本，集中处理文稿显示、语义缩略图与 macOS 程序坞图标。

## 修复内容

- 修复更新说明出现后，已打开文稿被遮挡或显示为空白的问题。更新说明改为非模态展示，同时支持 Esc 关闭、初始焦点与关闭后的焦点恢复。
- 保持语义缩略图放大框尺寸不变，将其中的预览文字统一缩小 3 px，使其回归缩略图的视觉尺度；预览仍会同步文稿选用的西文与中日韩字体。
- 修复 App Icon 的透明边缘与程序坞视觉尺寸。各档 ICNS 图像均保留真实透明通道，主体安全区与常见 macOS 应用图标保持一致。

## 发布范围

- 平台：macOS Apple Silicon（arm64）
- 签名：稳定标识的 ad-hoc 签名
- Apple 公证：未公证
- 更新链：继续使用 `latest-mac.yml`、ZIP SHA-512 与独立 SHA-256 校验文件
