# Reversion 2.1.4

2.1.4 修复侧栏收起后的恢复入口、默认分栏比例与「关于反文」白屏问题。

## 修复与调整

- 侧栏收起后，正文标题栏左上角保留展开按钮。按钮避开 macOS 窗口控制区，支持鼠标与键盘操作，展开后自动隐藏。
- 启动时恢复为 `1 : 3` 的侧栏与编辑区比例。当前会话仍可手动拖动细调；重新启动后回到规范比例。
- 「关于反文」改为应用内自有对话框，修复原先整页白屏的问题。对话框支持关闭按钮、点击遮罩关闭、Esc 关闭与焦点恢复。
- 保留 2.1.3 已完成的正文 76% 动态宽度、左右各 12% 留白、Claude-like 默认主题、文末 64 px 间距与表格字号减 2 px 规则。

## 发布范围

- 平台：macOS Apple Silicon（arm64）
- 签名：稳定标识的 ad-hoc 签名
- Apple 公证：未公证
- 更新链：继续使用 `latest-mac.yml`、ZIP SHA-512 与独立 SHA-256 校验文件
