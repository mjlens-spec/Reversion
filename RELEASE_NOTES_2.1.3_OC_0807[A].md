# Reversion 2.1.3

2.1.3 修正默认阅读布局与主题初始化，使启动状态、窗口自适应和实际正文边界保持一致。

## 修复与调整

- 侧栏启动时恢复为 `1 : 2.34` 的侧栏与编辑区比例。当前会话内仍可手动拖动；重新启动后回到规范比例。
- 正文文字区域改为动态占据可读区域的 76%，左右各保留 12%，不再使用固定像素上限。宽、窄窗口均会自动调整，且不会产生横向溢出。
- 全新配置默认进入 Claude-like 主题；已有配置中用户主动选择的主题不会被覆盖。
- 保留 2.1.2 已完成的文末 64 px 间距与表格字号减 2 px 规则。

## 发布范围

- 平台：macOS Apple Silicon（arm64）
- 签名：稳定标识的 ad-hoc 签名
- Apple 公证：未公证
- 更新链：继续使用 `latest-mac.yml`、ZIP SHA-512 与独立 SHA-256 校验文件
