# Reversion 1.5.0

表格编辑与链接编辑两条主线，对标 Typora 1.13.8。

## 表格

**右键菜单。** 在表格里点右键，多出「表格」子菜单：上方/下方插入行、左侧/右侧插入列、删除行、删除列、本列左/中/右对齐、复制表格为 Markdown / HTML、删除表格。这些能力此前只在悬浮工具条里，需要把指针停在列头上方或行首左侧的几像素内才会出现。菜单只在光标位于表格内时出现。

**Tab 到末格新建一行。** 此前在最后一个单元格按 Tab 没有反应，填表要么改用鼠标，要么每行按一次 ⌘+Enter。

**⌥↑ / ⌥↓ 移动表格行。** 拖拽手柄一直可以调整行序，但只能用鼠标。表头行不参与移动，也不会被顶替——GFM 没有无表头的表格，把正文行换到第一行会悄悄改变列名。

**宽表格在表格内横向滚动。** 此前一张 20 列的表会把整个文档撑出横向滚动条；两套内置主题为了塞下宽表把表格字号压到 13px，现在不再需要，表格字号回到正文字号。

**粘贴 TSV / CSV 自动成表。** 从 Excel、Numbers、Google Sheets 复制本来就会成表（它们同时提供 HTML 剪贴板内容）；从 `.csv` / `.tsv` 文件、终端或编辑器复制的纯文本此前原样落地。现在按 RFC 4180 解析（引号内的分隔符不拆列）。逗号是普通标点，所以逗号分隔的内容还需满足「有引号字段」或「列间无空格」才会转表，避免把「一, 二 / 三, 四」这样的正文误判成表格。代码块内、以及「粘贴为纯文本」不做转换。

## 链接

**行内链接可直接编辑。** 悬停链接弹出的工具条此前只有「移除链接」和「打开链接」两个按钮，改 URL 得回到源码里手改。现在多一个编辑按钮，展开「链接文字 / 链接地址」两个输入框；表单打开期间弹层会钉住，不会因为指针移开而消失。引用式链接 `[文字][标记]` 和 HTML `<a>` 不提供编辑入口——它们的源码形态不同，按 `[文字](地址)` 改写会悄悄改变文档结构。

工具条的两个图标从位图换成与格式工具条同套的 Material Symbols 矢量图标。

## 内部

- muya 引擎自带的 266 个 Playwright 用例首次纳入发布门禁（`npm run test:muya-e2e`）。
- 修复 Vitest 环境问题：Node 24 定义了一个未启用即为 undefined 的 `localStorage` 全局，导致 12 个桌面端单元测试文件在导入阶段就抛错、整批不计入统计。桌面端单元测试从 718 项（12 个文件静默未加载）恢复到 790 项全绿。

## 兼容性

- Apple Silicon（arm64）
- macOS 应用路径：`/Applications/Reversion.app`
- 用户数据目录继续使用 `~/Library/Application Support/marktext`
- Bundle ID 继续使用 `com.github.marktext.marktext`
- 可从 1.2.0 / 1.3.x 通过应用内更新直接升级

反文采用稳定 ad-hoc 签名，未经 Apple 公证。首次打开如被 Gatekeeper 阻止，请在访达中按住 Control 点击应用，再选择「打开」。
