# Reversion 反文 · 1.10.0 开发移交规划

日期：2026-08-06
上位文档：[开发规划_Claude_260726.md](开发规划_Claude_260726.md)（路线图）、[竞品调研_floralmd与界面对标_Claude_260806.md](竞品调研_floralmd与界面对标_Claude_260806.md)（本里程碑三个特性的调研依据）
用途：本文档为 1.10.0 里程碑的完整执行规划与节点验收标准，供移交 Codex 执行。执行者只需本文档 + 第八节速查即可开工，无需读完历史规划。

**里程碑定性（沿用 1.3.0 模式）：1.10.0 是可直接发布的正式版。** 不发公开 beta；所有阶段产出在本地/内部验证，最终一次性以正式 Release 发布。发布门禁见第七节，任何一项不过即不发布。

**里程碑主题**：吸收 FloralMD 调研中的三项设计——①中西文双字体独立设置、②Cmd+K 双语命令面板、③语义缩略图。三者相互独立，可并行推进。

---

## 一、现状回顾（截至 2026-08-06）

| 项 | 状态 |
| --- | --- |
| 已发布 | 1.9.0 正式版（2026-08-05）。基线：MarkText `v0.20.0-rc.1` + `@muyajs/core`（TS 引擎），Electron / Vue 3 |
| 应用源码 | `upstream/marktext`（gitignored 独立 git 仓，从未 push）。工具仓（本仓）存放主题/脚本/测试/发布管线 |
| 测试基线 | 单元 790 全绿（Node 24 修复后）；e2e 冒烟 + 品牌断言；muya 自带 266 个 e2e 用例纳入门禁；IME 矩阵（muya2） |
| 性能门禁 | 10k 行混合文档：输入延迟 P95 < 50ms、冷开 < 2s（1.3.0 起生效，本里程碑不得回退） |
| 主题体系 | 两套内置主题（lens-design、claude-like），各含编辑器 + 导出双目标；三字体槽位 `--reading-font-title/heading/body`，中文回退链写死在主题 CSS 内 |
| 工作区外壳 | 1.7.0–1.9.0 已完成：侧栏（文件树/搜索/目录）、常驻工作区栏、标签页、划选工具条、导出入口、标题栏文档路径 |
| 界面语言 | 10 种界面语言的 i18n 体系已存在（1.8.0 起新增文案均覆盖） |

**调研依据速引**（详见竞品调研文档 §1.3）：

- 双字体：FloralMD `Sources/FloralMDCore/Model/EditorTheme.swift:86-109`——西文字体定 metrics、中文字体只接未覆盖字符、「系统默认」时诚实显示实际生效字体。
- 命令面板：FloralMD `Sources/floralmd/App/CommandPaletteController.swift:161-170`——稳定命令 ID 目录被菜单/面板/快捷键共用；搜索同时索引中英文名 + 命令 ID + 快捷键；无文档时文档级命令置灰而非隐藏。
- 语义缩略图：FloralMD `Sources/floralmd/Views/DocumentMinimapView.swift`（163 行）——按块类型画结构色条而非微缩文字，叠加光标行与视口框。

## 二、1.10.0 范围定义

### 2.1 范围内

1. **F1 中西文双字体独立设置**：偏好设置新增「正文西文字体」「中文字体」两项，作用于编辑器与全部导出格式（HTML/PDF/PNG）；「跟随主题」时显示实际生效的中文字体。
2. **F2 Cmd+K 双语命令面板**：稳定命令 ID 目录 + 统一入口面板（命令 / 最近打开 / 当前文件夹文件），中英文双语索引搜索。
3. **F3 语义缩略图**：编辑区右缘的结构条 minimap（块类型着色 + 光标行 + 视口框，可点击/拖动导航），视图菜单与命令面板可开关。
4. **发布工程**：版本号、发布说明、README 双语更新、更新链路回验（标准检查清单，见第七节）。

### 2.2 可选加分项（不阻塞发布，完成三个主特性后经 Lens 批准方可动工）

- **A1 `text-autospace` 中西文自动间距**：编辑器容器与导出 HTML 启用 CSS `text-autospace`（M6 前置试点）。属性受 Chromium 支持即启用、不支持则静默跳过；不做 JS 回退（JS 回退属 M6 正式范围）。
- **A2 命令面板拼音首字母匹配**：中文命令名支持拼音首字母检索（如 `dcpdf` 命中「导出为 PDF」）。仅在不引入重量级依赖的前提下做（词表可预生成为纯数据模块）。

### 2.3 范围外（明确不做，执行中不得自行扩入）

- 斜杠菜单、悬浮便签/全局快速记录、Focus/打字机模式、Footnotes 侧栏（后续里程碑候选）。
- 三字体槽位的逐槽位西文覆盖（本版仅正文西文 + 全局中文两项；标题/小标题西文字体保持主题定义）。
- 界面（侧栏/菜单/工具条）字体设置——F1 仅作用于阅读字体槽位，不动界面字体。
- Quick Look 扩展的字体跟随（原生 Swift 管线，不在本版范围）。
- 菜单系统与命令目录的全量重构（本版只要求面板消费目录 + 建立 ID 映射表，见 S2 任务 2 的边界说明）。
- M2/M3 遗留（图片拖拽调宽、表格拖拽手柄）、M6 其余项、Apple 公证、Windows/Linux/Intel 构建。

## 三、工作分解

依赖关系：`S0 → (S1 ∥ S2 ∥ S3) → S4`。S1/S2/S3 相互独立，可并行或按顺序推进；建议顺序 S1 → S3 → S2（按规格确定性递减排序，S2 含最多待决项）。

### S0 · 现状盘点与规格冻结（开工必做，约 3 天）

按开发规划既定纪律：「里程碑开工前先做现状实测，不按旧认知直接排期」。产出统一落 `outputs/S0盘点_1.10.0_<日期>.md`，C0 评审通过后冻结范围。

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | 偏好体系盘点 | 定位偏好 schema、默认值、渲染进程读取路径与「三层加固」写入机制（1.2.0 引入）；确认字体类偏好现状（是否已有 `fontFamily` 类设置及其消费方式）。产出：偏好新增字段的落点方案 |
| 2 | 字体枚举能力盘点 | 确认主进程是否已有系统字体枚举依赖（MarkText 系历史上用 `font-list`）；无则评估引入。确认枚举结果经哪条 IPC 到达渲染进程。产出：字体枚举链路方案 |
| 3 | 主题变量注入点盘点 | 确认 `--reading-font-*` 三变量在编辑器内的注入方式（主题 CSS 静态定义 vs 运行时 style 注入），以及导出管线（HTML/PDF/PNG 共用路径）中导出主题 CSS 的拼装位置。产出：用户字体覆盖块的注入点清单（编辑器 1 处 + 导出 1 处） |
| 4 | 既有命令面板盘点 | 上游 MarkText 自 v0.16 起有命令面板（Cmd+Shift+P 线）。确认现代化分支中其存留状态、命令注册结构（主进程 commands 模块 / 渲染进程消费方式）、i18n 资源结构。产出：「升级现有面板 vs 重建」结论与理由 |
| 5 | 快捷键冲突排查 | 排查 `Cmd+K` 当前占用（Typora 系惯例为插入链接，冲突概率高）。产出冲突清单与两个候选方案：方案 A——面板占 `Cmd+K`，插入链接迁至 `Cmd+Shift+K`（对齐 Typora 用户肌肉记忆的代价评估）；方案 B——面板用 `Cmd+P`（文件优先）+ `Cmd+Shift+P`（命令优先）双入口，`Cmd+K` 不动。**C0 由 Lens 拍板** |
| 6 | Muya 块结构 API 盘点 | 确认从编辑器获取块级结构的途径：Muya state 树接口、内容变更事件订阅点、以及顶层块 DOM 节点的可枚举性（类名词表）。产出：F3 数据源方案（推荐 DOM 测量路线，见 §四.F3） |
| 7 | 右缘布局与命中测试盘点 | 复查 1.3.1「编辑区右侧死区」与 1.6.1「表格工具条盖弹窗」两次修复涉及的 hit-test 与 z-index 约定，给出 minimap 所处层级与命中规则草案 |
| 8 | 视觉稿 | F1 偏好设置字体区、F2 面板、F3 缩略图三处 UI 各出 ≥2 候选视觉稿（静态图或 HTML 原型均可），**先给 Lens 确认再实现**（沿用 B3 纪律）。主进程/IPC/数据结构不受此约束，可先行 |

### S1 · F1 中西文双字体独立设置（约 1–1.5 周）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | 偏好字段与持久化 | 新增两个偏好字段（命名沿用仓内既有风格，建议 `readingWesternBodyFont`、`readingCjkFont`），取值为字体族名字符串或哨兵值 `'theme'`（默认）。走既有三层加固写入；默认值下行为与 1.9.0 逐字节一致（迁移零感知） |
| 2 | 字体枚举与分类 | 主进程枚举系统字体 → IPC 送渲染进程；渲染进程用 `document.fonts.check('16px "<family>"', '中')` 逐族检测 CJK 覆盖，通过者进入「中文字体」候选列表；「西文字体」候选列表为全量。枚举与检测结果缓存于会话内，偏好窗口打开时异步加载不阻塞 |
| 3 | 字体栈合成器 | 纯函数 `composeReadingFontStack(slot, theme, prefs)`：对 title/heading/body 三槽位输出最终 font-family 栈。合成规则见 §四.F1 规格 F1-01～F1-04。**该函数必须是无副作用纯函数并配单元测试**（这是本特性的规格核心） |
| 4 | 编辑器侧注入 | 按 S0.3 确认的注入点，将合成结果以 `:root` 级覆盖块（`<style>` 或等价机制）注入编辑器，排在主题 CSS 之后。偏好变更即时生效，无需重启、不重载文档 |
| 5 | 导出侧注入 | HTML/PDF/PNG 导出的主题 CSS 拼装处追加同一合成结果的覆盖块。DOCX 不适用（pandoc 路线，Word 排版自理，与 1.6.0 决策一致），导出设置页不出现相关误导文案 |
| 6 | 「实际生效字体」解析 | `resolveEffectiveCjkFont(stack)`：沿最终栈顺序找到首个 `document.fonts.check(…, '中')` 通过的字体族。偏好界面在「跟随主题」选中时以次要文字显示「当前生效：<字体名>」；用户显式选择时不显示该行 |
| 7 | 偏好 UI | 按 Lens 确认的视觉稿实现：两个字体选择器（下拉 + 搜索过滤），每项以自身字体渲染预览文本（西文 `The quick brown fox 0123`；中文 `永和九年，岁在癸丑`）；首项为「跟随主题」。放置于偏好设置主题/字体分区 |
| 8 | 测试 | 单元：合成器全分支（见规格表）+ 解析器。e2e：改字体 → 编辑器 computed style 生效 → 导出 HTML 内含覆盖块 → 重启后偏好保持 → 恢复「跟随主题」后与内置主题快照一致。工具仓 `tests/` 契约测试补充：两套导出主题保留 `--reading-font-*` 变量（既有用例扩展） |

### S2 · F2 Cmd+K 双语命令面板（约 1.5–2 周）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | 命令目录模块 | 新建纯数据 + 薄执行层的命令目录：条目 schema 见 §四.F2 规格 F2-01。首批收录范围：文件（新建/打开/保存/另存/最近）、导出四格式、视图（侧栏三入口、源码模式、主题切换、缩略图开关）、格式（划选工具条既有项）、段落类型、窗口/偏好/检查更新。目标 ≥60 条，逐条含中英文名。**目录以稳定 ID 为键，ID 一经发布不得更名**（后续里程碑的快捷键自定义将依赖它） |
| 2 | 菜单映射表 | 不重构菜单系统；产出「菜单项 ↔ 命令 ID」映射表（纯数据，落于目录模块旁），面板执行命令走与菜单相同的既有分发通道（S0.4 确认的 commands/IPC 机制），保证同一命令两个入口行为逐字节一致。映射表配契约测试：每条目录条目必须能解析到真实处理器 |
| 3 | 面板 UI 与交互 | 按 Lens 确认的视觉稿实现覆盖层面板。行为规格 F2-02～F2-10（见 §四）。三分区：命令 / 最近打开 / 当前文件夹文件；输入即过滤；键盘上下/Enter/Esc；禁用命令置灰不隐藏 |
| 4 | 双语索引 | 每条命令的检索键 = 英文名 + 中文名 + 命令 ID + 快捷键串；与当前界面语言无关，中英文输入均可命中。文件分区检索键 = 文件名 + 相对路径 |
| 5 | IME 守卫 | 搜索框组合态（composition）期间 Enter 不执行选中项、Esc 先取消组合再关面板；过滤可随组合态实时进行。复用既有 IME 守卫模式，新增 e2e 用例入 IME 矩阵 |
| 6 | 快捷键接入 | 按 C0 拍板的方案（A 或 B）注册入口快捷键；若采方案 A，插入链接迁移需同步更新菜单加速键显示、划选工具条 tooltip 与 10 语言文案 |
| 7 | 测试 | 单元：目录完整性（ID 唯一、双语名非空、映射表全解析）、过滤排序函数。e2e：唤起/关闭、中文检索命中、英文检索命中、执行命令生效（抽 3 条代表：导出弹窗、侧栏切换、段落类型）、无文档时文档命令置灰、Esc 分层关闭与 1.7.0 既有层级不冲突 |

### S3 · F3 语义缩略图（约 1–1.5 周）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | 数据采集层 | 按 S0.6 确认的方案实现块摘要采集：枚举编辑器顶层块（标题级别、代码块、引用、列表及缩进深、表格、普通段落、分隔线），产出 `{type, depth, top, height}` 列表。推荐 DOM 测量路线：一次 rAF 内批量读 offsetTop/offsetHeight，缓存至下次失效；失效源 = 内容变更事件（debounce 150ms）+ 容器 resize + 主题/字体切换 |
| 2 | 渲染层 | 单 `<canvas>`，devicePixelRatio 适配。绘制规格 F3-01～F3-06（见 §四）。颜色全部取自当前主题的 CSS 变量（getComputedStyle 读取），主题/深浅色切换即重绘 |
| 3 | 交互层 | 点击跳转（内容滚动到对应位置，瞬时或短平滑二选一按视觉稿）、按住拖动连续滚动、视口框随编辑器滚动实时同步（passive scroll 监听，仅更新视口框图层，不触发全量重绘）。命中规则遵守 S0.7 约定：导出弹窗等模态打开期间缩略图不参与命中（复用 1.6.1 机制） |
| 4 | 开关与持久化 | 视图菜单新增「语义缩略图」勾选项 + 命令目录条目（依赖 S2 目录，若 S2 未就绪则先挂菜单、目录条目随 S2 补入）；偏好字段 `showMinimap` 持久化。默认值 C0 由 Lens 拍板（建议：默认开启，窗口宽度 < 900px 时自动隐藏不改偏好） |
| 5 | 性能验证 | 10k 行基准文档实测：开启缩略图后输入延迟 P95 仍 < 50ms、滚动不掉帧；采集耗时单独计量并写入报告。不达标则按序降级：加大 debounce → 块数超阈值（如 3000）时合并相邻同类块 → 超大文档自动停用并在状态处提示 |
| 6 | 测试 | 单元：块摘要 → 绘制指令的映射函数（纯函数化便于测试）。e2e：开关生效与持久化、结构条随内容变化更新（增删标题）、点击跳转位置正确（±1 视口容差）、视口框同步、无横向溢出、与右缘既有元素无命中冲突 |

### S4 · 发布工程（约 3 天）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | 版本与产物 | `package.json`（工具仓与 desktop 两处）升 1.10.0；`build-release-from-source.sh` 直出 arm64 DMG/ZIP/latest-mac.yml/sha256，归档 `releases/1.10.0/` |
| 2 | 发布说明与 README | 中文发布说明（更新弹窗渲染友好）；双语 README「更新内容」章节按 1.9.0 体例撰写三个特性 |
| 3 | 更新链路回验 | 本机装 1.9.0 → 指向 1.10.0 候选产物 → 完整走一遍更新流（发现/进度/确认/重启/更新说明仅一次）。回退验证：1.10.0 → 手动装回 1.9.0，新偏好字段不损坏旧版启动（未知字段容忍性实测） |

## 四、设计规格（验收基准，逐条可测）

### F1 双字体（规格表）

| 编号 | 规格 |
| --- | --- |
| F1-01 | 槽位合成顺序：`[body 且用户设了西文字体 → 用户西文字体]` + `主题西文链` + `[用户设了中文字体 → 用户中文字体]` + `主题中文回退链` + `通用族`。**用户中文字体必须插在全部西文字体之后、主题中文回退之前**（西文字形永远由西文字体先解析） |
| F1-02 | 两字段均为 `'theme'` 时，三槽位输出与 1.9.0 主题原始栈逐字符一致（含引号与顺序） |
| F1-03 | 用户西文字体仅影响 body 槽位；title/heading 槽位的西文部分永远保持主题定义（主题身份保护） |
| F1-04 | 字体族名含空格或非 ASCII 时输出必须带引号；来自 font-list 的名称原样使用，不做转写 |
| F1-05 | 偏好变更 → 编辑器视觉生效 ≤ 200ms，不重载文档、不丢光标与滚动位置、不打断 IME 组合态 |
| F1-06 | 导出 HTML/PDF/PNG 的字体覆盖块与编辑器同源（同一合成器输出），导出产物中可检出该覆盖块 |
| F1-07 | 「跟随主题」时显示实际生效中文字体名；所选字体在系统中被卸载后，下次解析自动落到链中下一可用族并如实显示 |
| F1-08 | 中文字体候选列表仅含 CJK 覆盖检测通过的字体族；检测不阻塞偏好窗口首帧 |

### F2 命令面板（规格表）

| 编号 | 规格 |
| --- | --- |
| F2-01 | 目录条目 schema：`{ id: string(稳定、点分层级，如 'file.export.pdf'), scope: 'app'\|'document', title: {en, zh}, keywords?: {en: string[], zh: string[]}, accelerator?: string, enabledWhen?: 谓词标识, run: 既有分发通道的调用描述 }`。纯数据与执行解耦，目录模块可被菜单映射表与面板同时 import |
| F2-02 | 唤起：全局快捷键（C0 拍板方案）任意界面可用；再次按下或 Esc 关闭；打开时输入框自动聚焦、内容清空、默认展示「最近使用命令 ≤5 + 最近打开文件 ≤5」 |
| F2-03 | 检索：输入对三分区并行过滤；命令区匹配范围 = en/zh 标题 + keywords + ID + 快捷键串；大小写不敏感；子串匹配即可（模糊算法不做硬性要求，排序规则：前缀命中 > 词首命中 > 子串命中，同级按最近使用降序） |
| F2-04 | 分区顺序固定：命令 → 最近打开 → 当前文件夹文件；空分区整组隐藏；每分区最多显示 8 条，可上下键连续跨区移动 |
| F2-05 | `scope: 'document'` 命令在无打开文档时置灰显示、不可执行、不隐藏；tooltip 说明原因 |
| F2-06 | Enter 执行当前高亮项并关闭面板；执行结果与从菜单触发同一命令逐字节一致（同一分发通道） |
| F2-07 | IME：组合态 Enter 不执行、Esc 先取消组合；组合中已上屏的拼音串参与过滤不报错 |
| F2-08 | Esc 分层：面板打开时 Esc 只关面板，不触达 1.7.0 既有的划选工具条/弹层关闭链 |
| F2-09 | 面板 UI 文案走既有 i18n（10 语言），但检索索引恒为 en+zh 双语，与界面语言无关 |
| F2-10 | 快捷键串在条目右侧以 macOS 惯例符号显示（⌘⇧K 式），来源为目录 accelerator 字段，与菜单显示一致 |

### F3 语义缩略图（规格表）

| 编号 | 规格 |
| --- | --- |
| F3-01 | 形态：编辑区右缘垂直条，内容宽度 12–16px（视觉稿定），上下贯通编辑区可视高度；不占用文档排版宽度、不引发横向滚动 |
| F3-02 | 着色映射（色值取主题 CSS 变量，禁止写死 hex）：标题=主题强调色（H1 最粗/最长，随级别递减）、代码块=次强调/橙系、引用=第三强调/紫系、表格=中性加深、普通段落=低对比中性、分隔线=细横线；列表按缩进深度做 x 偏移 |
| F3-03 | 纵轴映射：整篇文档高度线性映射到条高；文档很短时不拉伸（块条按真实比例聚在顶部） |
| F3-04 | 叠加层：当前光标所在块以高亮细线标记；当前视口以半透明框标记，框随滚动实时移动 |
| F3-05 | 交互：单击跳转对应位置；按住拖动连续滚动；悬停时鼠标为 pointer；不支持文本级精度（块级即可） |
| F3-06 | 重绘预算：内容变更后 debounce 150ms 重采集重绘；滚动仅更新视口框；单次全量重绘（10k 行文档）≤ 8ms，超标走 S3.5 降级序列 |
| F3-07 | 开关状态持久化；源码模式下缩略图自动隐藏（本版不为 CodeMirror 视图实现），切回渲染模式恢复 |
| F3-08 | 深浅色与主题切换后 300ms 内完成配色刷新 |

## 五、节点与验收标准

评审物沿用既有模式：diff（或 PR）+ 测试证据（命令与输出摘要）+ 报告（落 `outputs/`，文件名含任务号与日期）。评审人：Lens（终裁）+ Claude 会话（diff 级审查）。节点不过不进入下一阶段。

| 节点 | 时机 | 验收标准（全部满足才过） |
| --- | --- | --- |
| **C0 盘点与规格冻结** | S0 完成 | ① 盘点报告 8 项齐全、每项有代码证据；② Cmd+K 冲突方案、缩略图默认值两项 Lens 已拍板；③ 三处视觉稿已获 Lens 确认；④ 「升级 vs 重建」命令面板结论有依据。**此节点冻结范围与规格表** |
| **C1 F1 验收** | S1 完成 | ① 规格 F1-01～F1-08 逐条过（合成器单元测试覆盖 01–04 全分支）；② e2e 五场景绿；③ 默认值下与 1.9.0 视觉快照一致；④ 性能门禁不回退 |
| **C2 F3 验收** | S3 完成 | ① 规格 F3-01～F3-08 逐条过；② 10k 行实测报告达标（或降级序列已尽、Lens 裁决接受）；③ e2e 六场景绿；④ 与右缘既有元素/模态弹窗无命中冲突（1.6.1 场景回归） |
| **C3 F2 验收** | S2 完成 | ① 规格 F2-01～F2-10 逐条过；② 目录 ≥60 条、映射表契约测试全绿；③ 中英文检索 e2e 各命中；④ IME 用例入矩阵并绿；⑤ 若迁移了插入链接快捷键：菜单/工具条/10 语言文案三处同步完成 |
| **C4 发布门禁** | S4 完成 | 第七节检查清单逐项过 → Lens 批准发布 |

## 六、移交切分：Codex 与保留项

**建议 Codex 承接**（规格明确、验收可自动化）：

- S0.1–S0.7 全部盘点（产出报告供评审，不含拍板）
- S1 全部；S2 任务 1、2、4、5、7 与任务 3/6 的实现部分；S3 全部
- S4.1、S4.2 的产物准备

**保留给 Lens + Claude 会话**（决策、主观验收、设计确认）：

- C0–C4 全部节点评审与终裁
- S0.5 快捷键方案、S3.4 默认值、S0.8 视觉稿三项拍板
- S2.3 面板视觉与动效的最终验收（视觉稿一致性）
- S4.3 真机更新链路回验与发布操作

**协作协议**（沿用既有模式）：每个任务按简报执行，DoD = 简报内验收项全过 + 相关测试绿 + diff 审查无阻塞意见。Codex 产出一律以 diff/PR + 测试证据交付，不直接向 `reversion/main` 或 GitHub 推送；`upstream/marktext` 仓的分支操作限定在 `reversion/1.10`（从 1.9.0 发布 commit 切出）。执行报告写入 `outputs/`（gitignored，仅本地）。

**工期概览**：S0 约 3 天 → S1/S2/S3 并行或串行合计约 4 周 → S4 约 3 天。总计 **4–5 周**。若需压缩，优先砍 2.2 加分项，三个主特性不拆散（每个都是完整可发布单元，最坏情况允许 Lens 决策将 F2 顺延一个版本）。

## 七、发布检查清单（正式版门禁）

1. C0–C3 节点全部通过，无未裁决项。
2. 全量测试绿：单元契约（工具仓 + 应用仓）+ e2e 冒烟/品牌 + muya 266 用例 + IME 矩阵（含 F2 新增用例）。
3. 10k 行性能门禁复测通过（缩略图开启状态下）。
4. 更新链路回验 + 回退验证（新偏好字段容忍性）真机通过。
5. 版本号与产物：`package.json` 两处、DMG/ZIP/latest-mac.yml/sha256 齐备且版本一致；归档 `releases/1.10.0/`。
6. 中文发布说明就绪，更新弹窗实测渲染正常。
7. GitHub Release 以**正式版**发布——严禁勾选 pre-release（1.1.0 踩坑纪律）。
8. 发布后回验：线上 latest-mac.yml 可达，1.9.0 真机收到更新提示。
9. 双语 README、开发规划版本映射表、记忆文件同步更新。

## 八、执行者速查（Codex 开工必读）

**仓库结构**

- 本仓（工具仓）：品牌/主题/脚本/测试，无应用源码。应用源码在 `upstream/marktext`——独立 git 仓（被本仓 gitignore），分支从未 push，不得 push。
- 工作分支：`upstream/marktext` 内新建 `reversion/1.10`（从 1.9.0 发布线切出）；工具仓改动走常规分支流程。
- 本文档所有涉及上游源码的路径描述均为「盘点前假设」，**以 S0 实测为准**；S0 报告是后续一切任务的路径事实来源。

**常用命令**

```bash
npm test                      # 工具仓单元契约测试（node --test tests/*.test.mjs）
npm run test:e2e              # 应用级 e2e 冒烟 + 品牌断言
npm run test:muya-e2e         # 编辑器引擎自带 266 用例（Chromium）
REVERSION_IME_ENGINE=muya2 npm run test:e2e:ime   # IME 矩阵
bash scripts/build-release-from-source.sh 1.10.0   # 源码直出 DMG/ZIP/latest-mac.yml
```

**纪律红线**

1. 只构建 arm64；不做 Apple 公证（维持 ad-hoc 签名 + SHA-512/sha256 校验）。
2. 不擅自扩大范围：第 2.3 节范围外事项发现相关问题时记录到报告，不动手修；2.2 加分项未经 Lens 批准不得开工。
3. 命令 ID 一经 C3 验收即冻结，后续任何改名走弃用别名机制（本版无需实现别名机制，只需不改名）。
4. F1 默认值（双 `'theme'`）下产物与 1.9.0 视觉逐像素一致是硬要求；任何主题 CSS 的顺带「优化」都算范围外。
5. 面向用户的界面文本不得出现 MarkText 残留（白名单：帮助菜单上游支持入口）；新增文案覆盖全部 10 种界面语言；e2e 品牌断言保持绿。
6. IME 行为基准对齐 Typora 1.13.8；F2 搜索框必须过 IME 守卫用例。
7. 性能门禁（10k 行 P95 < 50ms / 冷开 < 2s）在缩略图开启状态下复测，不得回退。
8. 任何发布操作（tag、GitHub Release、更新源）仅由 Lens 执行。
