# Reversion 反文 · 1.3.0 开发移交规划

日期：2026-07-28
上位文档：[开发规划_Claude_260726.md](开发规划_Claude_260726.md)（版本映射）、[编程规划_Claude_260726.md](编程规划_Claude_260726.md)（E3/B3 任务表）
用途：本文档为 1.3.0 里程碑的完整执行规划与节点验收标准，供移交 Codex 执行其中部分任务。执行者只需本文档 + 第七节速查即可开工，无需读完历史规划。

**里程碑定性（Lens 决策，2026-07-28）：1.3.0 是可直接发布的正式版。** 不发公开 beta；所有阶段产出在本地/内部验证，最终一次性以正式 Release 发布。发布门禁见第六节，任何一项不过即不发布。

---

## 一、现状回顾（截至 2026-07-28）

| 项           | 状态                                                                                                                                                                                                                                    |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 已发布       | 1.2.0 正式版（2026-07-26），首个源码构建版本。门禁基线：单元测试 95 全绿 + e2e 19 全绿                                                                                                                                                  |
| 应用源码     | `upstream/marktext`（gitignored 独立 git 仓），分支 `reversion/main` = 上游 tag `v0.19.1` + **21 个本地 commit**（从未 push，MIT 不要求公开）                                                                                           |
| 当前引擎     | legacy `packages/muyajs`（`@marktext/muyajs`，JS + snabbdom）。上游三个 IME 修复分支全部作用于新引擎 `packages/muya`（`@muyajs/core`，TS 重写），对 legacy 无效——详见 `outputs/E3任务0_上游IME盘点_Claude_260726.md`                    |
| 引擎决策     | **直接迁移 `@muyajs/core`**（Lens 2026-07-26 拍板，已取消双引擎对照运行方案；双引擎测试基建保留用于验收对比）                                                                                                                           |
| IME 测试基建 | 已就绪：`tests-e2e/ime/` 矩阵 A/B/C + composition 模拟器；`tests-e2e/helpers/engine-profile.ts` 支持 `REVERSION_IME_ENGINE=legacy\|muya2`；`known-issues.ts` 按引擎注册缺陷（legacy 已映射 5+1 项，**muya2 表为空、只允许从实测填入**） |
| 上游状态     | 新引擎最新 tag `v0.20.0-rc.1`（commit `8f1d0a62`）；`origin/develop` 尖端 `43bd8b77`（2026-07-05，此后无推送）。IME 修复分支 `origin/fix/muya-ime-codeblock-rerender-4851`、`origin/fix/1250-ime-tab-composition` 未并入 rc.1           |

## 二、1.3.0 范围定义

### 2.1 范围内

1. **引擎迁移**：rebase 到上游 v0.20.0 线（基线 tag 见 S1），21 个本地 commit 逐个核销（移植 / 重做 / 废弃）。
2. **行内渲染 CSS 与主题迁移**：行内实时渲染 CSS 换 `mu-` 词表；两套内置主题（lens-design、claude-like）经 E2 转译器映射表交换再生；契约测试同步更新。
3. **IME 专项（新引擎验收）**：cherry-pick 上游未并入的 IME 修复 → 全矩阵映射 muya2 缺陷集 → 修复致命项至零已知复现。范围仅全拼 + 语音听写；行为基准对齐 Typora 1.13.8。
4. **长文档性能基准**：10k 行混合文档，输入延迟 P95 < 50ms、冷开 < 2s。
5. **B3 更新体验 UI**：下载进度条（含 Dock 进度）、更新完成说明弹窗、移除下载完成后的强制退出。任务表见编程规划 B3 节。
6. **遗留债清偿（正式版必须）**：主题 Google Fonts 死引用移除；B2 遗留的 V4（单实例锁）/ V5（真实更新链路彩排）/ V7（回退）验证。

### 2.2 范围外（明确不做，执行中不得自行扩入）

- E2 任务 4/5（GitHub/Night/Drake/Vue 四主题 A/B/C 分级 + 兼容文档）：可选加分项，不阻塞发布。
- M2 行内渲染 2.0、M3 表格编辑（1.4.0 / 1.5.0）。
- Apple 公证、Windows/Linux/Intel 构建、双拼/五笔/注音测试、应用图标源码化。
- `==高亮==` 等 Backlog 项（见开发规划第五节）。

## 三、工作分解

依赖关系：`S1 → S2 → S3 → S4`，`S5` 与 S2–S4 并行（依赖 S1），`S6` 收尾。

### S1 · 引擎迁移：新基线搭建

| #   | 任务               | 内容与产出                                                                                                                                                                                                                                                         |
| --- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | 基线锁定           | 默认基线 **tag `v0.20.0-rc.1`（`8f1d0a62`）**——确定性、可复现，且 develop 尖端此后无实质推进。开工时先核查上游是否已发 v0.20.0 stable：已发则改用 stable 并向 Lens 报备差异；未发则维持 rc.1。在 `upstream/marktext` 内新建分支 `reversion/1.3`（从基线 tag 切出） |
| 2   | 21 commit 核销清单 | 对 `v0.19.1..reversion/main` 全部 21 个 commit 逐个给出三态结论：**移植**（cherry-pick 干净或小改）/ **重做**（引擎耦合，如 `a95b17e3` 行内渲染 CSS、`69f5e7ee` legacy IME 守卫）/ **废弃**（新基线已含等价实现，注明上游 commit 号）。清单先评审后执行            |
| 3   | 移植执行           | 品牌类（productName、About、locale、链接指向）、偏好加固三层、自动更新源码化、默认 TOC/主题种子——按核销清单落到 `reversion/1.3`。commit 信息沿用上游 conventional commits 风格（英文）                                                                             |
| 4   | 构建跑通           | pnpm 安装 → 构建 → `scripts/build-release-from-source.sh` 直出 arm64 DMG/ZIP/latest-mac.yml。若构建脚本因 monorepo 结构变化需适配，一并修                                                                                                                          |
| 5   | 测试迁移           | `tests/` 契约测试与 `tests-e2e/` 冒烟 + 品牌断言在新构建上跑通；断言中引擎耦合的选择器按 `engine-profile.ts` 的 muya2 profile 调整                                                                                                                                 |

### S2 · 行内渲染 CSS 与主题迁移

| #   | 任务                    | 内容与产出                                                                                                                                  |
| --- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 词表映射表              | 提取新引擎 DOM 类名词表（`mu-` 前缀），与 legacy 词表形成映射表；同步更新 `scripts/typora-map` 中受影响的 selector 映射。映射表先评审后使用 |
| 2   | 行内渲染 CSS 重做       | 1.2.0 的行内实时渲染 CSS（原 `a95b17e3`）按映射表在新引擎上重做；渲染态/编辑态显隐行为与 1.2.0 对齐                                         |
| 3   | 两套主题再生            | lens-design、claude-like（编辑器 + 导出双目标）经转译器按新映射表再生；与 1.2.0 构建做截图对比，视觉一致                                    |
| 4   | Google Fonts 死引用清零 | 移除主题内 Google Fonts / jsDelivr `@import` 死引用；主题头部注释写明「安装霞鹜文楷」字体依赖说明。验收：应用内控制台无 CSP 拦截报错        |
| 5   | 契约测试更新            | `tests/` 主题相关契约测试（字体角色、表格、导出一致性）按新词表更新，全绿                                                                   |

### S3 · IME 专项（新引擎验收）

| #   | 任务              | 内容与产出                                                                                                                                                                                                                                                                                                                                   |
| --- | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 上游修复合入      | 核查 #4957（组合态 Backspace/Delete）、#4851（代码块组合态重渲染）、#1250（Tab 候选翻页）在基线 tag 的合入状态；未并入者从 `origin/fix/*` 分支 cherry-pick，冲突处按上游 PR 意图解决                                                                                                                                                         |
| 2   | muya2 缺陷集映射  | `REVERSION_IME_ENGINE=muya2` 跑全矩阵 A/B/C（用 `REVERSION_IME_KNOWN_EXTRA` / `REVERSION_IME_INTERMITTENT_EXTRA` 做发现循环），把实测缺陷集写入 `known-issues.ts` 的 `MUYA2_KNOWN` / `MUYA2_INTERMITTENT`（**只允许来自实测，禁止从 issue 文本预填**）。产出分级清单：P0 丢字/错位提交、P1 跳光标/重复字、P2 无用户可见损害的结构性 mutation |
| 3   | legacy 已知项复查 | legacy 6 项（A5、A6、B1@A3、B1@A9、B1@A10、B8@A3）在新引擎上逐项复测，结论并入分级清单                                                                                                                                                                                                                                                       |
| 4   | 修复批次          | 按分级清单修 P0/P1 至矩阵零已知复现；P2 项白名单化并在 `known-issues.ts` 注明理由。修复以最小 diff 落 `packages/muya`，可回馈上游的整理成 PR 草稿（是否提交由 Lens 决定）                                                                                                                                                                    |

### S4 · 长文档性能基准

| #   | 任务       | 内容与产出                                                                                                                             |
| --- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 基准脚手架 | 10k 行混合文档生成器（表格/代码块/公式/图片）+ 指标采集：输入延迟 P95、冷开时间、滚动帧率。可复用上游 `packages/muya/e2e` 的 perf 资产 |
| 2   | 测量与优化 | 新引擎实测。不达标则按性价比递进：渲染节流 → 视口外惰性渲染 → 超阈值降级提示。产出基准报告（达标数据，或差距 + 已尽路径说明）          |

### S5 · B3 更新体验 UI

任务表、IPC 契约、取数降级策略以编程规划 **B3 节**为准（主进程事件补齐 / 进度 UI / 完成弹窗 / 视觉文案 / 测试五项），此处不重复。两点执行约束：

1. **设计前置**：进度条与弹窗的视觉稿必须先给 Lens 再实现（候选方案 ≥2）。主进程事件、IPC、状态机、测试不受此约束，可先行。
2. **强退移除是硬要求**：`update-downloaded` 后必须等用户确认、且确认前走未保存文档检查。此项进入发布门禁。

### S6 · 发布工程与验证欠账

| #   | 任务                | 内容与产出                                                                                                                                                           |
| --- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | V4 单实例锁         | 新构建双开验证：第二实例聚焦已有窗口而非新开进程                                                                                                                     |
| 2   | V5 真实更新链路彩排 | 本机装 1.2.0 正式版 → 指向 1.3.0 候选产物的更新源 → 完整走一遍：发现更新 → 连续进度可见 → 可取消 → 下载完成确认（含未保存文档拦截）→ 重启 → 更新说明弹窗出现且仅一次 |
| 3   | V7 回退验证         | 1.3.0 → 手动装回 1.2.0：偏好文件不损坏、可正常启动（偏好三层加固应保证；实测确认）                                                                                   |
| 4   | 发布产物与文案      | arm64 DMG/ZIP + latest-mac.yml + sha256；中文发布说明（正文会被 B3 弹窗读取，格式按 Markdown 渲染友好写）；双语 README 更新                                          |

## 四、节点与验收标准

每个节点的评审物：diff（或 PR）+ 测试证据（命令与输出摘要）+ 报告（落 `outputs/`，文件名含任务号与日期）。评审人：Lens（终裁）+ Claude 会话（diff 级审查）。节点不过不进入下一阶段。

| 节点                  | 时机         | 验收标准（全部满足才过）                                                                                                                                                                                                               |
| --------------------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **C0 清单评审**       | S1.2 完成    | 21 commit 核销清单三态结论齐全、每项有依据（废弃项注明上游等价 commit）；S2.1 词表映射表就绪。**此节点冻结迁移范围**                                                                                                                   |
| **C1 新基线可用**     | S1 完成      | ① `reversion/1.3` 一条命令直出 DMG；② 应用可启动、开文档、编辑保存；③ 契约测试 + e2e 冒烟 + 品牌断言全绿（数量不低于 1.2.0 基线：单元 95 / e2e 19，因引擎变化删改的用例逐条列明理由）；④ 核销清单全部核销                              |
| **C2 主题与渲染对齐** | S2 完成      | ① 两套主题截图对比 1.2.0 视觉一致（差异逐项列明并获认可）；② 行内渲染显隐行为与 1.2.0 对齐；③ 控制台无 CSP 报错；④ 主题契约测试全绿                                                                                                    |
| **C3 IME 验收**       | S3 完成      | ① muya2 缺陷集分级清单经评审；② 修复后全矩阵零 P0/P1 已知复现（`REVERSION_IME_ENGINE=muya2` 完整跑绿，P2 白名单项有注明理由）；③ legacy 6 项复查结论齐全；④ 真机手工过一遍矩阵关键格位（全拼整句、代码块内组合、撤销后重输、语音听写） |
| **C4 性能与更新 UI**  | S4 + S5 完成 | ① 性能：10k 行 P95 < 50ms 且冷开 < 2s，或优化路径已尽、由 Lens 裁决是否接受为已知限制（裁决通过才算过）；② B3 单元 + e2e 全绿；③ 视觉稿已获 Lens 确认且实现与稿一致；④ 强退已移除，未保存拦截可复现                                    |
| **C5 发布门禁**       | S6 完成      | 第六节检查清单逐项过 → Lens 批准发布                                                                                                                                                                                                   |

## 五、移交切分：Codex 与保留项

**建议 Codex 承接**（规格明确、验收可自动化）：

- S1.2–S1.5：核销清单起草、移植执行、构建适配、测试迁移
- S2 全部：词表映射、CSS 重做、主题再生、死引用清理、契约测试更新
- S3.1–S3.2：上游修复 cherry-pick、矩阵发现循环与缺陷集映射
- S4.1：基准脚手架
- S5 中的主进程事件、IPC、状态机、单元/e2e 测试（编程规划 B3 任务 1、5）

**保留给 Lens + Claude 会话**（涉及决策、主观验收或设计确认）：

- C0–C5 全部节点评审与终裁
- S3.3–S3.4 的分级定级与修复方案决策（修复实现可再派给 Codex，按分级清单逐项派）
- S4.2 优化路径选择；S5 视觉稿（编程规划 B3 任务 2、3 的 UI 呈现与任务 4 视觉文案）
- S6 三项真机验证与发布操作

**协作协议**（沿用既有模式）：每个任务按简报执行，DoD = 简报内验收项全过 + 相关测试绿 + diff 审查无阻塞意见。Codex 产出一律以 diff/PR + 测试证据交付，不直接向 `reversion/main` 或 GitHub 推送；`upstream/marktext` 仓的分支操作限定在 `reversion/1.3`。执行报告写入 `outputs/`（gitignored，仅本地）。

## 六、发布检查清单（正式版门禁）

1. C1–C4 节点全部通过，无未裁决项。
2. 全量测试绿：单元契约 + e2e 冒烟/品牌 + IME 矩阵（muya2）；测试数量与删改理由已在 C1 记录。
3. V4 单实例锁、V5 真实更新链路彩排、V7 回退验证三项真机通过。
4. 版本号与产物：`package.json`（工具仓与 desktop 两处）、DMG/ZIP/latest-mac.yml/sha256 齐备且版本一致；产物归档 `releases/1.3.0/`。
5. 中文发布说明就绪，B3 弹窗实测渲染正常。
6. GitHub Release 以**正式版**发布——**严禁勾选 pre-release**（勾选会导致 1.1.0/1.2.0 用户收不到更新，1.1.0 时已踩过）。
7. 发布后回验：线上 latest-mac.yml 可达，1.2.0 真机收到更新提示。
8. 双语 README、`开发规划_Claude_260726.md` 版本映射表、记忆文件同步更新。

## 七、执行者速查（Codex 开工必读)

**仓库结构**

- 本仓（工具仓）：品牌/主题/脚本/测试，无应用源码。应用源码在 `upstream/marktext`——**独立 git 仓**（被本仓 gitignore），分支从未 push，不得 push。
- 上游远程：`marktext/marktext`（GitHub 重定向自 fork 前配置）；本地分支 `reversion/main`（v0.19.1 + 21 commits，1.2.0 发布线）、`reversion/1.3`（本里程碑工作线，S1.1 创建）。
- v0.20.0-rc.1 曾有构建 worktree 位于会话临时目录，可能已被清理；需要时用 `git -C upstream/marktext worktree add <path> v0.20.0-rc.1` 重建，勿依赖旧路径。

**常用命令**

```bash
npm test                      # 单元契约测试（node --test tests/*.test.mjs）
npm run test:e2e              # 应用级 e2e 冒烟 + 品牌断言
REVERSION_IME_ENGINE=muya2 npm run test:e2e:ime   # IME 矩阵（默认 legacy）
bash scripts/build-release-from-source.sh          # 源码直出 DMG/ZIP/latest-mac.yml
```

IME 矩阵发现循环环境变量：`REVERSION_IME_KNOWN_EXTRA` / `REVERSION_IME_INTERMITTENT_EXTRA`（逗号分隔格位）、`REVERSION_IME_OBSERVE_INTERMITTENT=1`、`REVERSION_IME_NO_ANNOTATIONS=1`。用法见 `tests-e2e/helpers/known-issues.ts` 注释。

**纪律红线**

1. 只构建 arm64；不做 Apple 公证（维持 ad-hoc 签名 + SHA-512/sha256 校验）。
2. `MUYA2_KNOWN` / `MUYA2_INTERMITTENT` 只允许从实测运行填入，禁止从上游 issue 文本推断预填。
3. IME 行为基准对齐 Typora 1.13.8；测试范围仅全拼 + 语音听写。
4. 面向用户的界面文本不得出现 MarkText 残留（白名单：帮助菜单「支持 MarkText」上游支持入口）；e2e 品牌断言必须保持绿。
5. 不擅自扩大范围：第 2.2 节范围外事项发现相关问题时记录到报告，不动手修。
6. 任何发布操作（tag、GitHub Release、更新源）仅由 Lens 执行。
