# Reversion 2.1.2

2.1.2 调整长文阅读布局，并统一表格字号在编辑器与样式化导出中的实际表现。

## 修复与调整

- 修复长文滚动到底部后仍有一整屏空白的问题。普通阅读模式改为保留 64 px 文末间距；打字机模式继续使用按视口计算的上下留白，输入居中行为不变。
- 扩大默认正文区域。宽窗口下版心外宽为 1300 px，扣除左右各 50 px 的编辑器内边距后，正文文字区域约为 1200 px；窄窗口下按 90% 宽度自适应，不产生横向溢出。
- 将表格字号统一设为当前正文字号减 2 px。该规则全局覆盖表头、单元格以及其中的加粗、行内代码等嵌套格式，并同步应用于 Lens Design、Claude-like 及其 HTML / PDF 样式化导出主题。

## 发布范围

- 平台：macOS Apple Silicon（arm64）
- 签名：稳定标识的 ad-hoc 签名
- Apple 公证：未公证
- 更新链：继续使用 `latest-mac.yml`、ZIP SHA-512 与独立 SHA-256 校验文件
