# Reversion 反文 · 开发规划 v1

日期：2026-07-26
基准：反文 1.1.0（Electron 42 / Vue 3 / muyajs / Mermaid 11.15 / KaTeX 0.16 / CodeMirror 5）
对标：Typora 1.13.8

## 一、范围决策记录

| 事项 | 决策 |
| --- | --- |
| 行内实时渲染 | 升级为重点方向，向 Typora 手感看齐 |
| 表格编辑 | 对标 Typora，高频功能，确认执行 |
| Typora 主题兼容 | 新增方向：做兼容层，接入其主题生态 |
| IME 专项、长文档性能（原 P0） | 确认执行 |
| 导出链路、表格（原 P1） | 确认执行 |
| 中文排印 | 纳入 P2 |
| GitHub Alerts | 暂缓 |
| 数学公式（KaTeX → MathJax v4、```math、\(\) 定界符） | 暂缓 |
| Apple 公证 | 不做（不购买开发者账号），维持 ad-hoc 签名 + SHA-512 校验现状 |
| Windows / Linux / Intel 构建 | 不做 |

## 二、工程前提（M0）：从补丁二进制到源码构建

**现状事实**

- 仓库不含应用源码。全部定制通过 `scripts/patch-asar-themes.mjs` 对预构建 `app.asar` 的压缩产物做锚点字符串替换（`replaceMarkedBlock` 等 4 个原语）+ CSS 注入完成。
- 该方式对主题/CSS 可持续；对内核级改动（IME、表格编辑、行内渲染行为、撤销栈）不可持续：锚点随上游构建漂移，JS 逻辑改动无法审阅、无法回归测试。
- M1、M2、M3 全部依赖 M0 完成。M4（主题兼容层）不依赖。

**任务**

1. 锁定上游：确认当前 `app.asar` 对应的上游仓库与 commit（MarkText 现代化分支 + muyajs workspace，package.json 维护者 Jocs / fxha / Tkaixiang），fork 为反文源码仓。
2. 建立可复现构建：源码 → `app.asar` → DMG/ZIP → `latest-mac.yml`，产物与现行 `build-release.sh` 对齐；ad-hoc 签名与自动更新链保持不变。
3. 迁移现有补丁为源码级修改：品牌、双语名称、主题注入、默认 TOC、`reversion-runtime.css`。`tests/` 现有 6 个契约测试全部迁移并保持通过。
4. 建立 e2e 能力：Playwright 驱动 Electron，作为后续所有编辑器行为规格的验收工具。

**验收**：一条命令从源码产出 DMG；全部测试通过；产出的应用与 1.1.0 行为一致。

**排期**：1–2 周。

**风险与退路**：若上游构建无法完全复现（第 1 周内确认），退路是保留 asar 补丁管线，但升级为「构建后处理脚本 + 快照 diff 测试」，并接受内核改动成本上升、M1–M3 排期放大。

## 三、里程碑

### M1 · 编辑内核品质：IME 与长文档（P0）

1. **IME 专项**
   - 测试矩阵：拼音整句输入、候选态中切换段落/点击他处、标点自动配对与 IME 冲突、行内代码与公式边界输入、撤销后重输、双拼、五笔、日文假名。
   - 修复目标：矩阵内零已知复现 bug。
   - 自动化：e2e 模拟 composition 事件序列；每版本发布前跑手工矩阵。
2. **长文档性能**
   - 基准文档：5,000 / 10,000 行混合内容（表格、代码块、公式、图片）。
   - 指标与阈值：10k 行输入延迟 P95 < 50ms；冷开 < 2s；滚动不掉帧。
   - 优化路径（按性价比递进）：块级渲染节流 → 视口外块惰性渲染 → 超阈值降级提示（建议切源码模式）。
3. **撤销/粘贴保真**（次优先，随 IME 修复顺带处理）：撤销粒度规格化；网页/Word/飞书粘贴转 Markdown 的还原度回归集。

**排期**：3–4 周。第 1 周先出矩阵与量化结论，若实测 IME 问题少于预期，压缩本里程碑提前进入 M2。

### M2 · 行内实时渲染 2.0

现状：Muya 原生 hide/show + `reversion-runtime.css` 零宽折叠，已达形态对齐；到 Typora 级手感需要行为级（JS）改动。先对照 Typora 逐项写行为规格表，再实现。

1. **显隐粒度**：只展开光标所在行内语法范围，同段其他语法保持渲染态（当前 active 为段落级）。
2. **布局稳定**：标记展开/收起时，光标所在字符保持屏幕位置不跳动（可接受 reflow，不可接受视觉跳变）。
3. **光标映射**：点击渲染态元素落点准确；方向键跨行内元素不跳字、不丢位。
4. **链接体验**：渲染态 Cmd+Click 打开；URL 与显示文字分离编辑的弹层。
5. **行内代码与公式**：进入显示原文、退出即渲染，编辑期间不闪烁。
6. **图片**：选中态、拖拽调整宽度并写回文档（本里程碑末位，可顺延）。

**验收**：行为规格表全项通过 e2e。
**排期**：3–4 周，依赖 M0。

### M3 · 表格编辑（对标 Typora）

1. **单元格导航**：Tab / Shift+Tab / Enter / 方向键行为规格化。
2. **行列操作**：悬浮工具条 + 右键菜单——前后插入行/列、删除、列对齐三态。
3. **重排**：行/列拖拽手柄；Alt+↑/↓ 移动行，并推广至任意段落/块级（对标 Typora 1.11）。
4. **粘贴导入**：TSV/CSV/Excel 粘贴自动成表；表格复制为 Markdown/HTML。
5. **宽表格**：编辑器内横向滚动容器，替换当前主题里压缩字号（13px）的临时方案。

**排期**：3–4 周，依赖 M0。与 M2 共享块级选区/光标工作，顺序 M2 → M3。

### M4 · Typora 主题兼容层（不依赖 M0，可并行先行）

**可行性依据**：`themes/claude-like-marktext.css` 即一次人工移植（原版在 `.tmp/claude-source/`），映射关系已验证：

| Typora | 反文 |
| --- | --- |
| `#write`（内容根） | `.mu-container`（编辑器）/ `.markdown-body`（导出） |
| `:root` 变量（`--bg-color` `--text-color` `--accent-color` `--code-*` 等） | 反文变量体系 + 三字体槽位 `--reading-font-*` |
| 元素级选择器（h1–h6 / blockquote / table / code） | 大多直通 |
| Typora 专有 UI（`#typora-sidebar`、`.megamenu`、导出专用类） | 剥离 |

**任务**

1. 转译器 `scripts/import-typora-theme.mjs`（postcss）：输入 Typora 主题 CSS（含字体资源目录），输出编辑器主题 + 导出主题 + 兼容报告（列出被丢弃/无法映射的规则）。
2. 用 3–5 个流行主题验证（GitHub、Night、Drake、Vue、Claude-like），建立 A（完全兼容）/ B（基本兼容）/ C（不兼容）分级。
3. 阶段二：偏好设置内「导入 Typora 主题」入口；兼容主题目录页文档。

**风险管理**：重度依赖 Typora DOM 结构或自带 JS 的主题还原度低——用分级与报告管理预期，不承诺 100% 还原。
**排期**：MVP（CLI + 5 主题验证）1.5–2 周。

### M5 · 导出链路（P1)

1. **PDF**：大纲书签；分页控制（page-break 样式 + 规格化分页符段落）；页眉页脚模板（页码/标题/日期变量）。
2. **docx**：检测 pandoc → 引导安装（brew）→ 无缝调用；不打包二进制（体积与许可考虑）。
3. **一致性契约**：导出主题与编辑器主题一致性测试从表格扩展到全要素（现有 `tests/` 已有基础）。

**排期**：2 周。

### M6 · 中文排印（P2）

1. **中西文自动间距**：渲染层实现（CSS `text-autospace` 优先，按支持度回退 JS 渲染时处理），不改写用户源文件。
2. **标点**：行首行尾禁则、标点悬挂、连续标点挤压——按引擎支持度取舍。
3. **智能引号**：中文语境自动弯引号配对；破折号、省略号输入辅助。
4. **复制到微信公众号格式保持**（对标 Typora 1.11，中文写作者高频场景）。

**排期**：2–3 周。

## 四、版本映射与执行顺序

| 版本 | 内容 | 说明 |
| --- | --- | --- |
| 1.2.0 ✅ **已发布 2026-07-26** | M0 + M4 MVP + 品牌一致性清理（B1）+ productName 迁移（B2）+ 偏好加固 | 源码构建地基 + 主题转译器 + 品牌闭环（菜单栏/Dock 显示 Reversion）+ 偏好写入三层加固 + IME 删除键守卫。发布页：https://github.com/mjlens-spec/Reversion/releases/tag/v1.2.0 |
| 1.3.0 | 引擎迁移（Lens 2026-07-26 决策：直接迁移 `@muyajs/core`，取消双引擎对照）+ M1 收尾 + **B3 更新体验 UI** | rebase 到上游 v0.20.0 线，定制重做（行内渲染 CSS/主题映射表交换/行为默认值），cherry-pick 上游 IME 修复，IME 矩阵与性能基准在新引擎上验收；另加更新体验 UI——下载进度条（含 Dock 进度）、更新完成说明弹窗、移除下载完成后的强制退出（详见编程规划 B3 节） |
| 1.4.0 | M2 | 行内渲染 2.0（新引擎上实施） |
| 1.5.0 | M3 | 表格编辑（新引擎上实施） |
| 1.6.0 | M5 | 导出链路 |
| 1.7.0 | M6 + M4 阶段二 | 中文排印 + 应用内主题导入 |

排序逻辑：M4 不依赖源码构建，先行并拿到对外可见成果；内核三件套按「先稳定（M1）、再手感（M2）、再高频功能（M3）」推进；总排期在 M1 第 1 周量化结论后修正一次。

## 五、暂缓清单（Backlog）

- GitHub Alerts、```math 代码块、\(\) 数学定界符、MathJax v4
- `==高亮==` 语法（E2 核实：muyajs v0.19.1 分词器不支持，属内核能力缺口）
- Apple 公证（决策：不做）
- Windows / Linux / Intel / Universal 构建（当前管线仅 arm64）
- 版本历史 / 未保存草稿恢复（观察）
- 主题外部字体自托管（E1 遗留：Lens Design 主题的 Google Fonts / jsDelivr `@import` 在应用内被 CSP 拦截、大陆环境不可达，目前是死引用+控制台噪音。短期先移除死引用并写明「安装霞鹜文楷」的字体依赖说明，长期子集化 woff2 自托管。建议排入 1.2.0 或 1.2.x）
- 应用图标源码化（目前打包后覆盖，E1 遗留）
- 自动更新实链路验证（静态推导已 18 项全过；真实升级链路在 1.2.0 正式发布时顺带验证，列入发布检查清单而非 backlog）

## 六、执行方式

每个里程碑三段式：**规格**（对照 Typora 逐项写行为规格表，先评审）→ **实现** → **e2e 验收**。本周可立即启动的三件事：M0 上游 commit 锁定调查、M4 转译器原型、M1 IME 测试矩阵草案。
