# Reversion 反文 · 2.1.0 开发移交规划

日期：2026-08-06（同日修订 R1：并入原 2.2.0 候选——明暗双配色、复制到微信公众号；新增界面与布局调整组 F4，含大纲命名、侧栏行距、缩略图加宽与悬停放大器、表格字号）
上位文档：[开发规划_Claude_260726.md](开发规划_Claude_260726.md)（路线图，本版覆盖 M6 主体）、[1.10.0开发移交规划_AC_260806.md](1.10.0开发移交规划_AC_260806.md)（2.0.0 实际执行的规划，格式沿用）、[竞品调研_floralmd与界面对标_Claude_260806.md](竞品调研_floralmd与界面对标_Claude_260806.md)
用途：本文档为 2.1.0 里程碑的完整执行规划与节点验收标准，供移交 Codex 执行。执行者只需本文档 + 第九节速查即可开工。

**里程碑定性：2.1.0 是可直接发布的正式版。** 发布门禁见第八节，任何一项不过即不发布。

**里程碑主题：中文排印 + 沉浸写作 + 界面精修。** 六个特性组：①中文排印一期、②排印设置面板、③专注与打字机模式、④界面与布局调整（Lens 指定四项）、⑤主题明暗双配色一体化、⑥复制到微信公众号。竞争依据见第二节。

**规模提示（Lens 知悉）**：R1 修订后本版为大型里程碑，估算总工期 8–10.5 周（见第七节）。若执行中需要压缩，剪裁顺序建议见第七节末——剪裁决定仅由 Lens 作出。

---

## 一、现状回顾：2.0.0 验收结论（2026-08-06）

2.0.0 已发布并通过验收（验收执行：Claude 会话，基于工具仓 + GitHub Release 可验证范围）：

| 验收项 | 结论 |
| --- | --- |
| 工具仓契约测试 | 99 项全绿（容器内 40 项因 `upstream/marktext` 不在场 skip，属预期） |
| GitHub Release v2.0.0 | 正式版（非 pre-release）；DMG/ZIP/双 sha256/latest-mac.yml 五件齐全；latest-mac.yml 版本与 ZIP 大小逐字节一致，更新链完整 |
| 对照 1.10.0 规划 | F1 双字体、F2 命令面板、F3 语义缩略图全部落地；三个 C0 决策点均有结论（面板走 `Cmd+Shift+P`、`Cmd+K` 留给目录面板；缩略图默认开启 + <900px 自动隐藏） |
| 性能门禁 | P95 36.2ms / 冷开 1.661s / 60fps 滚动，缩略图开启状态下达标 |
| 超规划交付 | 页面新形态（浮层侧栏、Mermaid 容器、四色 highlight、引用块重做），有两轮视觉 QA 记录 |
| **发现缺陷（已修复）** | 两份导出主题各含一个悬空 `}`（2.0.0 改平面 highlight 时旧闭括号未删净；Chromium 容错不影响渲染，严格工具链会报错）。已修复并新增覆盖全部五份样式表的括号配平契约测试（工具仓测试增至 100 项），落在分支 `claude/floralmd-reversion-comparison-jfanpi`，**待合入 main（列入本版发布门禁）** |
| 未能远程验证 | 应用仓 871 项单元测试与真机行为（以 Lens 本机发布记录为准） |

## 二、竞争定位：为什么 2.1.0 做「中文排印 + 沉浸写作 + 界面精修」

三个直接对标的最新状态（调研日期 2026-08-06，来源见竞品调研文档与本节链接）：

1. **Typora 1.14.x**（2026-07 起）：半年节奏是小步迭代——浮动编辑工具栏、文件树键盘导航与隐藏文件显示、MathJax v4、Mermaid 11.12。**更新日志中零中文/CJK 排版投入，无内置 AI。** 中文排印仍是 Typora 的空白区。
2. **Lettera beta**（Bear 团队，2026-06 公测，尚无正式版/定价/移动端）：**首个 beta 更新（2026-07-01）就是 Typography 设置（字体/字号/行高/段距）+ Mermaid + 打字机模式**——文件型 Markdown 编辑器用户把排印设置投成了第一优先。其打字机模式尚有选区 bug；社区高频缺口（自定义主题、专注模式、自动化）官方表态非优先。
3. **FloralMD**：CJK 深度止步于双字体级联，无中西文间距、无标点挤压、无禁则。

结论：**中文排印是三家共同的空白，而反文 2.0.0 恰好建成了它的地基**（双字体栈在编辑器与导出共用的注入管线）。打字机/专注模式已成品类 table stakes，本版补齐并借 Typora 的细粒度开关做得更好。R1 并入的两项原 2.2.0 候选各有依据：**明暗双配色**是 Lettera 社区高频缺口（其官方表态非优先）与反文自身深色模式短板（1.3.1 曾修）的交汇点；**复制到微信公众号**对标 Typora 1.11 同名能力，是中文写作者高频场景、直接服务本版「中文写作」定位。F4 界面调整为 Lens 对 2.0.0 真机使用后的直接反馈，全部落为可测规格（见第五节）。

## 三、2.1.0 范围定义

### 3.1 范围内

1. **F1 中文排印一期**：
   - F1a 中西文自动间距（CSS `text-autospace`，渲染层，不改源文件）
   - F1b 标点挤压（CSS `text-spacing-trim`，渲染层）
   - F1c 智能引号（中文语境输入 `"` / `'` 自动成对弯引号「“”‘’」，输入层）
   - F1d 一键中文自动排版（显式命令，规范化**源文件**：中英空格、全半角标点统一）
2. **F2 排印设置面板**：字号、行高、段落间距、正文栏宽四项阅读排印设置，主题值为默认、用户可覆盖，编辑器与导出共用（复用 2.0.0 双字体注入管线）。
3. **F3 专注与打字机模式**：段落级专注淡出 + 打字机滚动（含「仅输入时居中」细粒度开关）。
4. **F4 界面与布局调整（Lens 指定，R1 新增）**：
   - F4a 侧栏「目录」入口更名「大纲」（图标 + 顶部标题 + 全部关联文案，10 语言）
   - F4b 侧栏行距缩小至现值约 50%（文件树、搜索结果、大纲条目统一）
   - F4c 语义缩略图内容宽度加倍（16px → 32px）
   - F4d 缩略图悬停放大器（悬停弹出半透明浮层，显示悬停位置的实际渲染内容；克制精致、迅捷优雅）
   - F4e 正文表格字号自动降一级（正文字号 − 1px，跟随 F2 字号设置联动）
5. **F5 主题明暗双配色一体化（原 2.2.0 候选，R1 并入）**：两套内置主题各配成套暗色变体；外观三态（浅色/深色/跟随系统）；全部 2.0 浮层组件（侧栏、面板、缩略图、放大器）取主题变量。
6. **F6 复制到微信公众号（原 2.2.0 候选，R1 并入）**：一条命令把整篇/选区复制为公众号编辑器可直接粘贴的内联样式 HTML。
7. **发布工程**：含 2.0.0 验收遗留（悬空括号修复分支合入）。

### 3.2 可选加分项（不阻塞发布，主特性完成后经 Lens 批准方可动工）

- **A1 命令面板拼音首字母检索**（2.0.0 规划遗留项；词表预生成为纯数据模块，不引入重量级依赖）。
- **A2 标点悬挂**（CSS `hanging-punctuation` 按引擎支持度做，不支持则放弃，不做 JS 模拟）。

### 3.3 范围外（明确不做，执行中不得自行扩入）

- 悬浮便签/全局快速记录、Footnotes 侧栏、斜杠菜单、句子级专注（后续里程碑候选）。
- 行首行尾禁则的 JS 兜底（CSS `line-break: strict` 由 S0 实测现状，合格则记录结论关闭）。
- Typora 导入主题的自动暗色化（F5 仅覆盖两套内置主题；导入主题在深色外观下的回退策略由 C0 拍板，默认回退内置暗色，不做逐主题转换）。
- 导出主题的暗色变体（HTML/PDF/PNG 导出维持浅色；打印品不做深色）。
- 公众号之外的平台适配（知乎/掘金等剪贴板格式，观察反馈后再议）。
- PDF 大纲书签与分页控制（M5 遗留，另行排期）。
- Apple 公证、Windows/Linux/Intel 构建。

## 四、工作分解

依赖关系：`S0 → (S1 ∥ S2 ∥ S3 ∥ S4) → S5 → S6 → S7`。其中 **S5（明暗主题）排在 S4（界面调整）之后**——放大器、加宽缩略图、侧栏新行距都要进暗色 token 体系，先定形再上色；**S6（公众号）依赖 S1 完成**——内联 HTML 需带上 F1a/F1b 的排印效果（以内联可表达的近似为准）。建议执行顺序：S1 → S2 → S4 → S3 → S5 → S6。

### S0 · 现状盘点与规格冻结（约 4–5 天）

产出统一落 `outputs/S0盘点_2.1.0_<日期>.md`，C0 评审通过后冻结范围。

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | CSS 排印属性支持度实测 | 在应用实际 Electron/Chromium 版本内实测四项：`text-autospace`、`text-spacing-trim`、`line-break: strict`、`hanging-punctuation`（`CSS.supports` + 渲染截图各一）。**若 `text-autospace` 不支持，F1a 降级为仅导出 HTML 侧启用并报 Lens 裁决** |
| 2 | 禁则现状实测 | 中文行首「，。」、行尾「「（」复现文档实测；合格则关闭该项 |
| 3 | 智能引号输入链路盘点 | 定位 Muya 标点自动配对实现与 IME 守卫方式；确认弯引号插入对撤销栈、光标的影响路径 |
| 4 | 一键排版改写通道盘点 | 确认「整篇/选区程序化替换 + 单步撤销」的现有 API 与光标/滚动保持策略 |
| 5 | 排印变量注入点复查 | 复查 2.0.0 双字体覆盖块机制，确认字号/行高/段距/栏宽四变量同机制追加可行；盘点两套主题四项当前取值 |
| 6 | 滚动与光标基建盘点 | 编辑器滚动容器、光标 viewport 定位 API、缩略图视口同步通道；专注模式活动块信号源（Muya active 类名） |
| 7 | 缩略图实现盘点（F4c/F4d 前置） | 通读 2.0.0 缩略图实现：canvas 绘制粒度、命中层、<900px 隐藏与模态停止命中的机制。评估放大器内容源两条路线并给结论：**路线甲**——对应区域块 DOM 克隆 + CSS transform 缩放（真实渲染、成本低、需处理克隆节点的样式隔离）；**路线乙**——离屏渲染位图（隔离彻底、实现重）。产出推荐路线与理由 |
| 8 | 侧栏文案与行距落点盘点 | 「目录」字样全部出现位置清单（侧栏入口、顶部标题、视图菜单、命令面板条目、tooltip、10 语言资源、e2e 断言文本）；侧栏三视图（文件/搜索/大纲）条目行高与 padding 的当前值与选择器落点（注意 1.8.1 的 8px 边距与字号调整为既有基准） |
| 9 | 表格字号落点盘点 | 两套编辑器主题 + 两套导出主题中表格字号选择器现状（注意 1.5.0 已移除 13px 压缩、导出侧保留 13px 的历史——见开发规划 M3.5 与既有契约测试「editor and export themes balance narrow and wide tables」，本项改动必须同步更新该测试） |
| 10 | 主题 token 与外观监听盘点（F5 前置） | 清点两套主题全部颜色定义（CSS 变量化程度、写死 hex 清单）；确认现有深色模式行为（1.3.1 修复的机制）、`nativeTheme`/`prefers-color-scheme` 监听现状；产出 token 化改造清单与工作量评估 |
| 11 | 公众号粘贴实测（F6 前置） | 用手工内联 HTML 样张在微信公众号编辑器实测并记录：内联样式保留范围、`<img>` dataURI 是否被接收并转存、代码块/表格/引用/KaTeX 公式 HTML 的实际表现、Mermaid SVG 与 PNG 的表现。**此项是 F6 规格的事实基础，实测由 Lens 配合（需公众号后台）** |
| 12 | 偏好与命令目录扩容点 | 新增偏好字段清单与命令目录新条目清单（专注、打字机、一键排版、排印面板、外观三态、公众号复制），沿用稳定命令 ID 体系 |
| 13 | 视觉稿 | 四处 UI 各出 ≥2 候选视觉稿，先给 Lens 确认再实现：①F2 排印面板；②F1d 一键排版确认对话框；③F4d 放大器浮层（半透明质感、圆角、阴影、弹出动效参数——含一版「与 2.0.0 浮层侧栏同语言」方案）；④F5 两套主题的暗色配色样张（编辑器全要素页 + 侧栏 + 缩略图） |

### S1 · F1 中文排印一期（约 1.5–2 周）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | F1a/F1b 渲染层启用 | 编辑器容器与导出主题启用 `text-autospace` 与 `text-spacing-trim`（终值 C0 冻结，倾向 `normal` + `space-first`）。偏好总开关 `cjkTypographyEnabled`（默认开启，C0 确认）。两套内置主题与 Typora 导入主题产物均生效 |
| 2 | F1c 智能引号 | 按 S0.3 挂接点实现：中文语境（规格 F1-05）输入 `"`/`'` 插入成对弯引号、光标居中；IME 组合态不触发；单步撤销。偏好开关 `smartCjkQuotes`（默认关闭，C0 确认） |
| 3 | F1d 一键排版命令 | 「中文排版整理」命令（命令面板 + 格式菜单）：全文/选区执行规格 F1-07 规则集；确认对话框含规则复选与替换计数；单步撤销；豁免区块零改动 |
| 4 | 导出一致性 | HTML/PDF/PNG 验证 F1a/F1b 生效；DOCX 不适用且无误导文案 |
| 5 | 测试 | 单元：语境判定、规则集全正反例。e2e：开关持久化、智能引号（含 IME 用例入矩阵）、一键排版与撤销、导出含排印属性 |

### S2 · F2 排印设置面板（约 1 周）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | 偏好字段 | `readingFontSize`、`readingLineHeight`、`readingParagraphSpacing`、`readingColumnWidth`，哨兵值 `'theme'`（默认）。三层加固写入 |
| 2 | 注入实现 | 四项合成入 2.0.0 同一 `:root` 覆盖块（编辑器 + 导出双注入点）；「恢复主题默认」只重置本分区（与双字体分组独立，规格 F2-04） |
| 3 | 面板 UI | 按视觉稿实现，与双字体同区组成「排印」分区：字号 12–24px、行高 1.3–2.2（步进 0.05）、段距 0–2em、栏宽 600–1200px 或「适应窗口」；实时预览 |
| 4 | 测试 | 单元：合成器扩展分支。e2e：即时生效、重启保持、恢复默认回主题快照、导出含覆盖值、与双字体互不干扰 |

### S3 · F3 专注与打字机模式（约 1–1.5 周）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | 专注模式 | 非活动块降不透明度（值取主题变量，暗色下同样可读）；≤200ms 淡入过渡；与划选工具条、表格工具条、缩略图、放大器共存无冲突 |
| 2 | 打字机模式 | 光标行保持视口垂直中线；子开关「仅输入时居中」（默认开）；IME 组合态不回中、`compositionend` 后一次性对齐 |
| 3 | 状态与互斥 | 两模式独立、可同开；持久化；源码模式下暂停生效；视图菜单打勾 |
| 4 | 测试 | e2e：开关持久化、淡出跟随、两种回中行为差异断言、IME 组合视口稳定、10k 行 + 双模式性能复测 |

### S4 · F4 界面与布局调整（约 1–1.5 周）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | F4a 大纲更名 | 按 S0.8 清单全量替换：侧栏入口图标（换用层级列表语义的图标，与现有 Material Symbols 线框语言一致）、入口文字、面板顶部标题、视图菜单项、命令面板条目双语名（**命令 ID 不改**，仅显示名）、tooltip；10 语言资源同步（zh-Hans「大纲」、zh-Hant「大綱」、en「Outline」，其余语言按对应概念词）。**语义边界**：仅改「侧栏标题大纲面板」相关文案；文档内 `[TOC]`、导出设置里的「目录」（文档内目录）语义不同，**不改** |
| 2 | F4b 侧栏行距 | 文件树、搜索结果、大纲条目三处行距统一缩小至现值约 50%（精确值按视觉稿定，落点按 S0.8）：条目上下 padding 减半、行高相应收紧；保持 1.8.1 的 8px 内容区边距不变；条目命中高度不得低于 20px（可点性底线）；键盘导航、选中态、悬停态、禁用态视觉全部复验 |
| 3 | F4c 缩略图加宽 | 内容宽度 16px → 32px；结构条粗细、缩进偏移、Git 式标记（如有）按新宽度重新标定（等比放大会显粗，需按视觉稿微调）；<900px 自动隐藏阈值联动复查（加宽后是否上调阈值由视觉稿定）；正文栏宽（F2）与缩略图共存无横向溢出 |
| 4 | F4d 悬停放大器 | 按 S0.7 推荐路线实现。行为规格 F4-05～F4-10（见 §五）。浮层视觉按视觉稿：半透明表面 + 背景模糊、与 2.0.0 浮层侧栏同语言的圆角/描边/阴影，「收敛、精致」为验收词——禁止大面积高透明度导致底下文字透叠花掉 |
| 5 | F4e 表格字号 | 四份主题（两套编辑器 + 两套导出）表格单元格字号改为相对值 `calc(正文字号 − 1px)`（或等价 em 值），随 F2 字号设置联动；表头同步；行高相应微调保持密度；**同步更新既有契约测试**「editor and export themes balance narrow and wide tables」（该测试记录了导出侧 13px 的历史决策，本项改动后以「正文 − 1px」为新契约） |
| 6 | 测试 | 单元：主题契约测试更新（表格字号相对化、大纲文案断言）。e2e：大纲入口与面板标题文案（10 语言抽 3）、侧栏行距快照对比、缩略图新宽度渲染、放大器弹出/跟随/消失/模态抑制、表格字号随 F2 联动 |

### S5 · F5 主题明暗双配色一体化（约 1.5–2 周，依赖 S4 定形）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | token 化改造 | 按 S0.10 清单把两套主题写死的颜色全部收敛为语义 token（表面/文字/强调/线条/代码底/高亮四色等）；浅色值与 2.0.0 逐像素一致（这是回归基线） |
| 2 | 暗色变体 | 按 Lens 确认的暗色样张为两套主题定义暗色 token 集；覆盖全要素：正文、代码、表格、引用、四色 highlight（暗色下重新标定透明度）、Mermaid 容器、侧栏浮层、缩略图、放大器、专注模式淡出层、划选工具条 |
| 3 | 外观三态 | 偏好新增「外观」：浅色/深色/跟随系统（默认跟随系统，C0 确认）；跟随系统时监听系统外观实时切换（≤300ms 完成，含缩略图重绘）；手动态优先于系统 |
| 4 | 边界处理 | Typora 导入主题在深色外观下的回退策略（C0 拍板，默认：导入主题视为浅色专用，深色外观下继续使用该主题但界面 chrome 走暗色——或整体回退内置暗色，二选一冻结）；导出恒为浅色（3.3 节既定） |
| 5 | 测试 | 单元：token 完整性契约（暗色集与浅色集键名一一对应、无写死 hex 残留）。e2e：三态切换、系统外观联动、暗色下全要素截图套（与样张对比）、缩略图/放大器取色正确 |

### S6 · F6 复制到微信公众号（约 1–1.5 周，依赖 S1、S0.11 实测）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | 内联化管线 | 「复制为公众号格式」命令（命令面板 + 编辑/文件菜单）：以导出 HTML 管线为源，执行样式内联化（选择器 → style 属性），产出剪贴板 `text/html`（同时保留 `text/plain` 为 Markdown 源）。范围：整篇或当前选区 |
| 2 | 要素适配 | 按 S0.11 实测结论逐要素处理：本地图片 → dataURI（或按实测结论调整）；Mermaid/图表 → PNG dataURI；KaTeX 公式 → PNG dataURI（公众号无 MathML/外部脚本）；代码块 → 带背景色的内联样式（按实测选高亮方案）；表格/引用/highlight → 内联样式保真 |
| 3 | 主题与排印 | 内联样式取当前导出主题浅色值 + F2 排印覆盖；F1a 间距以内联可表达的近似呈现（实测 `text-autospace` 在公众号内是否存活，不存活则接受降级并在文档注明） |
| 4 | 体量与反馈 | 复制过程给进度/完成反馈（长文 + 多图 dataURI 体量大）；超阈值（如 20MB）给出明确提示而非静默失败 |
| 5 | 测试 | 单元：内联化器（选择器展开、dataURI 替换、豁免处理）。e2e：命令执行后剪贴板含 text/html 与关键内联片段断言；真机粘贴到公众号编辑器的最终验收由 Lens 执行（C6 项） |

### S7 · 发布工程（约 3 天）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | 验收遗留合入 | 分支 `claude/floralmd-reversion-comparison-jfanpi`（悬空括号修复 + 括号配平契约测试 + 调研/规划文档）合入 main |
| 2 | 版本与产物 | 2.1.0 版本号两处、DMG/ZIP/latest-mac.yml/sha256，归档 `releases/2.1.0/` |
| 3 | 发布说明与 README | 中文发布说明；双语 README（核心功能新增「中文排印」与「明暗主题」条目；「目录」相关截图与文案更新为「大纲」） |
| 4 | 更新链路回验 | 2.0.0 → 2.1.0 真机更新流全程；回退验证（新偏好字段容忍性） |

## 五、设计规格（验收基准，逐条可测）

### F1 中文排印（规格表）

| 编号 | 规格 |
| --- | --- |
| F1-01 | F1a/F1b 为纯渲染层效果：开关任意切换，文档源文件字节不变（e2e 断言保存后文件 hash 不变） |
| F1-02 | 总开关关闭时，渲染与 2.0.0 基线视觉一致（回归快照） |
| F1-03 | F1a 在编辑器、HTML、PDF、PNG 四处生效且视觉一致；Typora 导入主题产物同样生效 |
| F1-04 | F1a/F1b 开启不影响光标命中与选区（混排行点击落点、双击选词、方向键与关闭时一致） |
| F1-05 | F1c 中文语境判定：光标前最近非空白字符为 CJK 字符或 CJK 标点 → 中文语境；文档开头/行首默认非中文语境；行内代码、代码块、公式、链接 URL 内永不触发 |
| F1-06 | F1c 插入 `“”` 后光标居中；紧接输入 `"` 不再次配对；中间为空时 Backspace 删除整对；单步撤销 |
| F1-07 | F1d 规则集 v1（每条独立复选，默认全选）：① CJK 与拉丁/数字间补半角空格（标点相邻处豁免）；② 中文语境半角 `,.!?:;()` 转全角（数字千分位/小数点/版本号豁免）；③ 连续空格收敛为一（行首缩进与 Markdown 语法性空格豁免）；④ 全角空格转半角 |
| F1-08 | F1d 豁免区块（代码块、行内代码、公式、链接/图片 URL、YAML front matter、HTML 块）零改动；单步撤销；对话框展示各规则替换计数 |
| F1-09 | F1d 10k 行文档执行 ≤ 2s，UI 不冻结 |
| F1-10 | F1c IME 组合态不触发；关闭时输入行为与 2.0.0 逐字节一致 |

### F2 排印面板（规格表）

| 编号 | 规格 |
| --- | --- |
| F2-01 | 四项默认 `'theme'` 时渲染与 2.0.0 基线逐像素一致；切主题跟随新主题值 |
| F2-02 | 覆盖后编辑器 ≤200ms 生效、不重载文档、不丢光标滚动；导出用同一覆盖值 |
| F2-03 | 栏宽与侧栏/缩略图（F4c 新宽度）共存无横向溢出，窄窗口自动让位 |
| F2-04 | 「恢复主题默认」只重置本分区四项，不触碰双字体；反之亦然 |
| F2-05 | 数值边界在 UI 与偏好写入双层钳制；越界偏好文件启动回落且不崩溃 |
| F2-06 | 行高/段距对代码块、公式块不产生破坏性叠加（豁免选择器明确）；表格行高遵循 F4e 规格 |

### F3 专注与打字机（规格表）

| 编号 | 规格 |
| --- | --- |
| F3-01 | 专注：非活动块降不透明度至主题 token 值（浅/暗色下正文均可辨识，对比度 ≥ 3:1）；活动块与内联工具不受影响 |
| F3-02 | 活动块切换淡出跟随 ≤200ms，无闪烁；图片/图表/表格块同样参与 |
| F3-03 | 打字机（子开关关）：光标移动后光标行回视口垂直中线（±0.5 行）；文档首尾行为按 C0 冻结的方案执行 |
| F3-04 | 打字机（「仅输入时居中」开，默认）：自由滚动不回拉；键入/回车/Backspace 后回中 |
| F3-05 | IME 组合期间不回中（e2e 断言组合期间 scrollTop 零变化）；`compositionend` 后一次性对齐 |
| F3-06 | 源码模式下两模式暂停、菜单置灰、状态保留，切回恢复 |
| F3-07 | 双模式开启下 10k 行门禁不回退；专注淡出用 CSS 类切换，禁止逐块 JS 样式写入 |
| F3-08 | 新命令沿用稳定 ID（`view.focusMode`、`view.typewriterMode`、`format.cjkTidy`、`preferences.typography`、`view.appearance`、`edit.copyForWeChat`），双语名齐备、面板可检索 |

### F4 界面与布局调整（规格表）

| 编号 | 规格 |
| --- | --- |
| F4-01 | 「大纲」更名覆盖 S0.8 清单全部位置；10 语言资源齐备；e2e 品牌与文案断言同步更新后全绿；文档内 `[TOC]` 与导出设置「目录」不受影响（语义边界断言） |
| F4-02 | 大纲入口图标更换后与侧栏其余图标同语言（线框、同视觉重量、几何居中——1.7.2 的对齐纪律） |
| F4-03 | 侧栏三视图条目行距为 2.0.0 现值的 50%±10%（终值按视觉稿冻结）；条目命中高度 ≥20px；8px 内容区边距不变；选中/悬停/禁用态与键盘导航全部复验；同窗口高度下可见条目数近似翻倍（快照对比） |
| F4-04 | 缩略图内容宽度 32px；结构条视觉按视觉稿重标定（非简单 2 倍拉伸）；标题/代码/引用/表格在 32px 下肉眼可分辨类型；点击/拖动导航精度不劣于 2.0.0；<900px 隐藏机制照旧（阈值若上调按 C0 冻结值） |
| F4-05 | 放大器触发：鼠标进入缩略图区域悬停 ≥150ms 弹出（防误触）；移出缩略图或开始拖动视口框即隐藏；模态弹窗打开期间不弹出（沿用 2.0.0 停止命中机制） |
| F4-06 | 放大器内容：显示悬停 Y 对应文档位置附近区域的**实际渲染内容**（真实字体、主题样式），缩放比例使正文在浮层内可读（≥11px 等效字号）；随鼠标 Y 移动连续跟随更新，无肉眼卡顿（跟随更新 ≤1 帧延迟） |
| F4-07 | 放大器视觉：半透明表面 + 背景模糊，透明度以「浮层下方内容仅作氛围、不干扰浮层文字辨识」为准（对比度 ≥ 4.5:1）；圆角、描边、阴影与 2.0.0 浮层侧栏同语言；浅/暗色主题下均取 token |
| F4-08 | 放大器动效：弹出 ≤120ms 完成（淡入 + 轻微位移，无弹跳）；消失 ≤100ms；连续悬停移动期间浮层不重播入场动效 |
| F4-09 | 放大器为纯展示层：不参与命中（点击穿透到缩略图）、不可选中文字、不含交互控件；打开期间输入延迟门禁不回退（悬停态不影响编辑性能，隐藏态零成本） |
| F4-10 | 放大器与窗口边界：浮层完整可见（贴缩略图内侧弹出，位置按视觉稿冻结）；窗口过窄不足以容纳时不弹出而非截断 |
| F4-11 | 表格字号 = 当前正文字号 − 1px（含表头），随 F2 字号设置实时联动；编辑器与 HTML/PDF/PNG 导出一致；行高微调后表格密度提升但不与 F2-06 冲突；既有宽表格横向滚动行为不变；相关契约测试更新后全绿 |

### F5 明暗双配色（规格表）

| 编号 | 规格 |
| --- | --- |
| F5-01 | token 化后浅色渲染与 2.0.0 逐像素一致（回归基线）；主题 CSS 无写死 hex 残留（契约测试断言，白名单需注明理由） |
| F5-02 | 暗色 token 集与浅色键名一一对应；暗色下全要素（正文/代码/表格/引用/四色 highlight/Mermaid 容器/侧栏/缩略图/放大器/专注淡出/划选工具条）与 Lens 确认的样张一致 |
| F5-03 | 外观三态：手动浅色/深色恒定生效；跟随系统时系统切换 ≤300ms 完成全界面（含缩略图重绘、放大器 token） |
| F5-04 | 暗色下正文对比度 ≥ 7:1，次要文字 ≥ 4.5:1；四色 highlight 在暗色下重新标定透明度使文字可读 |
| F5-05 | Typora 导入主题在深色外观下按 C0 冻结的回退策略执行，无花屏/不可读组合 |
| F5-06 | 导出（HTML/PDF/PNG）恒为浅色，不受外观状态影响（e2e 断言深色外观下导出产物与浅色外观下逐字节一致） |

### F6 复制到公众号（规格表）

| 编号 | 规格 |
| --- | --- |
| F6-01 | 命令执行后剪贴板含 `text/html`（内联样式，无 class/id 依赖、无外部资源引用）与 `text/plain`（Markdown 源）双格式 |
| F6-02 | 粘贴到公众号编辑器后（真机验收）：标题层级、粗斜体、代码块（含背景与等宽字体）、表格、引用、highlight、有序/无序列表样式保留，视觉与导出主题浅色一致度以 S0.11 实测结论为基准 |
| F6-03 | 本地相对路径图片内联为 dataURI（或 S0.11 实测更优方案）；Mermaid/图表/KaTeX 公式转 PNG dataURI，分辨率 2 倍 |
| F6-04 | 选区复制只含选区内容；无选区时默认整篇（行为与导出一致） |
| F6-05 | 复制期间有进度反馈；完成有明确成功提示；产物超过阈值（C0 冻结，建议 20MB）时给出提示与继续/取消选择 |
| F6-06 | 剪贴板写入失败、图片读取失败等异常路径均有用户可理解的错误提示，不静默失败、不写入残缺 HTML |

## 六、节点与验收标准

评审物沿用既有模式：diff/PR + 测试证据 + 报告落 `outputs/`。评审人：Lens（终裁）+ Claude 会话（diff 级审查）。节点不过不进入下一阶段。

| 节点 | 时机 | 验收标准（全部满足才过） |
| --- | --- | --- |
| **C0 盘点与规格冻结** | S0 完成 | ① 盘点报告 13 项齐全（CSS 属性支持度有截图、放大器路线有结论、公众号实测有记录）；② 待决项全部拍板：排印属性终值与默认开关、F1c 默认关、F3-03 首尾行为、缩略图隐藏阈值、放大器弹出位置、外观默认三态、导入主题暗色回退、F6 体量阈值；③ 四处视觉稿（含暗色样张）获 Lens 确认 |
| **C1 F1 验收** | S1 完成 | 规格 F1-01～F1-10 逐条过；规则集单元全分支；IME 新用例入矩阵绿；导出四路一致；源文件零改动断言过 |
| **C2 F2 验收** | S2 完成 | 规格 F2-01～F2-06 逐条过；默认值下与 2.0.0 逐像素一致；边界钳制实测过 |
| **C3 F4 验收** | S4 完成 | 规格 F4-01～F4-11 逐条过；大纲文案 10 语言抽查；侧栏行距与缩略图/放大器真机走查获 Lens 视觉认可；表格字号契约测试更新后全绿；放大器开启悬停状态下性能门禁不回退 |
| **C4 F3 验收** | S3 完成 | 规格 F3-01～F3-08 逐条过；10k 行 + 双模式性能复测达标；真机全拼输入打字机无跳动 |
| **C5 F5 验收** | S5 完成 | 规格 F5-01～F5-06 逐条过；浅色回归基线逐像素通过；暗色全要素截图套与样张一致获 Lens 认可；三态切换 e2e 绿 |
| **C6 F6 验收** | S6 完成 | 规格 F6-01～F6-06 逐条过；**Lens 真机粘贴公众号编辑器走查通过**（含长文多图样张）；异常路径逐条实测 |
| **C7 发布门禁** | S7 完成 | 第八节检查清单逐项过 → Lens 批准发布 |

## 七、移交切分与工期

**建议 Codex 承接**：S0.1–S0.10、S0.12 盘点；S1–S6 全部实现与测试；S7.2、S7.3 产物准备。

**保留给 Lens + Claude 会话**：C0–C7 节点评审与终裁；S0.11 公众号实测（需后台配合）；S0.13 视觉稿与全部待决项拍板；F1d 对话框中文措辞审校；C3/C5 的真机视觉走查；C6 公众号真机验收；S7.1 分支合入、S7.4 真机回验与发布操作。

**协作协议**（沿用既有模式）：任务按简报执行，DoD = 简报验收项全过 + 测试绿 + diff 审查无阻塞；Codex 产出以 diff/PR + 测试证据交付，不直接推送；`upstream/marktext` 分支操作限定 `reversion/2.1`；执行报告写入 `outputs/`（gitignored）。

**工期概览**：S0 约 4–5 天 → S1/S2/S3/S4 合计约 4.5–6 周 → S5 约 1.5–2 周 → S6 约 1–1.5 周 → S7 约 3 天，总计 **8–10.5 周**。

**若需压缩（剪裁仅由 Lens 决定），建议顺序**：加分项 A1/A2 → F1c 智能引号（顺延一版）→ F6 公众号（顺延一版，S0.11 实测结论保留）→ F5 明暗主题（顺延一版，token 化改造 S5.1 可先行落地为纯重构）。F1a/F1b/F1d、F2、F4 是本版定位与 Lens 直接反馈项，不建议剪裁。**也可考虑拆为 2.1.0（F1–F4）与 2.2.0（F5–F6）两个正式版发布**——好处是 Lens 反馈的界面调整更快到达用户；是否拆分由 Lens 在 C0 前决定，本文档两种走法均适用（拆分时 S5/S6 章节整体平移为 2.2.0 规划）。

## 八、发布检查清单（正式版门禁）

1. C0–C6 节点全部通过，无未裁决项。
2. 全量测试绿：工具仓契约（≥100 项，含括号配平与本版更新的表格字号/大纲文案契约）+ 应用仓单元 + e2e 冒烟/品牌 + muya 用例 + IME 矩阵（含 F1c/F3 新用例）。
3. 10k 行性能门禁在最重状态（缩略图加宽 + 放大器悬停 + 专注 + 打字机 + 暗色外观）复测通过。
4. 2.0.0 验收遗留分支已合入 main。
5. 更新链路回验 + 回退验证真机通过（新偏好字段容忍性，含外观三态字段）。
6. 版本号与产物齐备一致，归档 `releases/2.1.0/`。
7. 正式版发布，严禁 pre-release；发布后回验 latest-mac.yml 可达、2.0.0 真机收到更新提示。
8. 双语 README（中文排印、明暗主题条目；「目录」→「大纲」的截图与文案）、开发规划版本映射表、记忆文件同步更新。

## 九、执行者速查（Codex 开工必读）

**仓库结构**：本仓（工具仓）存主题/脚本/测试/发布管线；应用源码在 `upstream/marktext`（gitignored 独立仓，不得 push）；本里程碑工作分支 `reversion/2.1`（从 2.0.0 发布线切出）。本文档涉及上游源码的描述均为盘点前假设，**以 S0 实测为准**。

**常用命令**

```bash
npm test                      # 工具仓契约测试（现 100 项）
npm run test:e2e              # 应用级 e2e 冒烟 + 品牌断言
npm run test:muya-e2e         # 引擎自带用例（Chromium）
REVERSION_IME_ENGINE=muya2 npm run test:e2e:ime   # IME 矩阵
bash scripts/build-release-from-source.sh 2.1.0    # 源码直出发布产物
```

**纪律红线**

1. 只构建 arm64；不做 Apple 公证。
2. 不擅自扩大范围：3.3 节范围外事项只记录不动手；加分项未经 Lens 批准不得开工。
3. F1a/F1b 是渲染层特性，**任何改写用户源文件的行为都算缺陷**；F1d 是唯一允许改源文件的路径且必须单步撤销 + 豁免区块零改动。
4. 排印四项与双字体全部 `'theme'`、外观为浅色时，产物与 2.0.0 视觉逐像素一致是硬要求（F4 各项按其规格的新基线为准）。
5. 新增命令走稳定命令 ID 体系，双语名齐备，已发布 ID 不得改名（F4a 只改显示名）。
6. IME 行为基准对齐 Typora 1.13.8；F1c 与 F3 的组合态守卫用例必须入矩阵。
7. 性能门禁在「缩略图加宽 + 放大器悬停 + 专注 + 打字机 + 暗色」最重状态下复测。
8. 面向用户界面文本无 MarkText 残留；新文案覆盖 10 种界面语言；「目录」→「大纲」不得漏改也不得越界改（F4-01 语义边界）。
9. 任何发布操作仅由 Lens 执行。

## 十、后续版本候选（不属于本版承诺）

- 悬浮便签 + 全局快速记录（FloralMD 借鉴项）；Footnotes 侧栏（Obsidian 2025）；斜杠菜单；句子级专注（iA Writer）；命令面板拼音检索（若本版加分项未做）。
- 知乎/掘金等其他平台的复制适配（视 F6 反馈）。
- **观察项**：Lettera 正式版发布与定价（当前仍 beta、无主题系统、无移动端——反文的窗口期）；MarkText 上游 2026 年恢复维护后的 IME/光标修复回合排查。
