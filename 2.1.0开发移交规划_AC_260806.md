# Reversion 反文 · 2.1.0 开发移交规划

日期：2026-08-06
上位文档：[开发规划_Claude_260726.md](开发规划_Claude_260726.md)（路线图，本版对应 M6 主体）、[1.10.0开发移交规划_AC_260806.md](1.10.0开发移交规划_AC_260806.md)（2.0.0 实际执行的规划，格式沿用）、[竞品调研_floralmd与界面对标_Claude_260806.md](竞品调研_floralmd与界面对标_Claude_260806.md)
用途：本文档为 2.1.0 里程碑的完整执行规划与节点验收标准，供移交 Codex 执行。执行者只需本文档 + 第九节速查即可开工。

**里程碑定性：2.1.0 是可直接发布的正式版。** 发布门禁见第八节，任何一项不过即不发布。

**里程碑主题：中文排印 + 沉浸写作。** 三个特性——①中文排印一期（自动间距/标点挤压/智能引号/一键排版）、②排印设置面板、③专注与打字机模式。竞争依据见第二节。

---

## 一、现状回顾：2.0.0 验收结论（2026-08-06）

2.0.0 已发布并通过验收（验收执行：Claude 会话，基于工具仓 + GitHub Release 可验证范围）：

| 验收项 | 结论 |
| --- | --- |
| 工具仓契约测试 | 99 项全绿（容器内 40 项因 `upstream/marktext` 不在场 skip，属预期） |
| GitHub Release v2.0.0 | 正式版（非 pre-release）；DMG/ZIP/双 sha256/latest-mac.yml 五件齐全；latest-mac.yml 版本与 ZIP 大小逐字节一致，更新链完整 |
| 对照 1.10.0 规划 | F1 双字体、F2 命令面板、F3 语义缩略图全部落地；三个 C0 决策点均有结论（面板走 `Cmd+Shift+P`、`Cmd+K` 留给目录；缩略图默认开启 + <900px 自动隐藏） |
| 性能门禁 | P95 36.2ms / 冷开 1.661s / 60fps 滚动，缩略图开启状态下达标 |
| 超规划交付 | 页面新形态（浮层侧栏、Mermaid 容器、四色 highlight、引用块重做），有两轮视觉 QA 记录 |
| **发现缺陷（已修复）** | 两份导出主题各含一个悬空 `}`（2.0.0 改平面 highlight 时旧闭括号未删净；Chromium 容错不影响渲染，严格工具链会报错）。已修复并新增覆盖全部五份样式表的括号配平契约测试（工具仓测试增至 100 项），落在分支 `claude/floralmd-reversion-comparison-jfanpi`，**待合入 main（列入本版发布门禁）** |
| 未能远程验证 | 应用仓 871 项单元测试与真机行为（以 Lens 本机发布记录为准） |

## 二、竞争定位：为什么 2.1.0 做「中文排印 + 沉浸写作」

三个直接对标的最新状态（调研日期 2026-08-06，来源见竞品调研文档与本节链接）：

1. **Typora 1.14.x**（2026-07 起）：半年节奏是小步迭代——浮动编辑工具栏、文件树键盘导航与隐藏文件显示、MathJax v4、Mermaid 11.12。**更新日志中零中文/CJK 排版投入，无内置 AI。** 中文排印仍是 Typora 的空白区。
2. **Lettera beta**（Bear 团队，2026-06 公测，尚无正式版/定价/移动端）：**首个 beta 更新（2026-07-01）就是 Typography 设置（字体/字号/行高/段距）+ Mermaid + 打字机模式**——文件型 Markdown 编辑器用户把排印设置投成了第一优先。其打字机模式尚有选区 bug；社区高频缺口（自定义主题、专注模式、自动化）官方表态非优先。
3. **FloralMD**：CJK 深度止步于双字体级联，无中西文间距、无标点挤压、无禁则。

结论：**中文排印是三家共同的空白，而反文 2.0.0 恰好建成了它的地基**（双字体栈在编辑器与导出共用的注入管线）。同时，打字机/专注模式已成为该品类的 table stakes（Typora、iA Writer、Ghostwriter 已有，Lettera 刚补），反文应在本版补齐并借 Typora 的细粒度开关做得更好。故 2.1.0 = M6 主体提前落地 + 沉浸写作补课，打出「中文排印最认真的 Markdown 编辑器」的定位。

## 三、2.1.0 范围定义

### 3.1 范围内

1. **F1 中文排印一期**：
   - F1a 中西文自动间距（CSS `text-autospace`，渲染层，不改源文件）
   - F1b 标点挤压（CSS `text-spacing-trim`，渲染层）
   - F1c 智能引号（中文语境输入 `"` / `'` 自动成对弯引号「“”‘’」，输入层）
   - F1d 一键中文自动排版（显式命令，规范化**源文件**：中英空格、全半角标点统一）
2. **F2 排印设置面板**：字号、行高、段落间距、正文栏宽四项阅读排印设置，主题值为默认、用户可覆盖，编辑器与导出共用（复用 2.0.0 双字体注入管线）。
3. **F3 专注与打字机模式**：段落级专注淡出 + 打字机滚动（含「仅输入时居中」细粒度开关）。
4. **发布工程**：含 2.0.0 验收遗留（悬空括号修复分支合入）。

### 3.2 可选加分项（不阻塞发布，三个主特性完成后经 Lens 批准方可动工）

- **A1 命令面板拼音首字母检索**（2.0.0 规划遗留项，如 `dcpdf` 命中「导出为 PDF」；词表预生成为纯数据模块，不引入重量级依赖）。
- **A2 标点悬挂**（行尾句号/逗号悬挂出栏，CSS `hanging-punctuation` 按引擎支持度做，不支持则放弃，不做 JS 模拟）。

### 3.3 范围外（明确不做，执行中不得自行扩入）

- 复制到微信公众号格式保持（M6 原条目，独立成后续版本；涉及剪贴板 HTML 内联化，工程量与本版主轴不匹配）。
- 悬浮便签/全局快速记录、Footnotes 侧栏、斜杠菜单、句子级专注（后续里程碑候选）。
- 主题明暗双配色一体化（2.2.0 候选主轴）。
- 行首行尾禁则的 JS 兜底（CSS `line-break: strict` 由 S0 实测现状，若引擎已默认合格则记录结论即可，不写 JS）。
- PDF 大纲书签与分页控制（M5 遗留，另行排期）。
- Apple 公证、Windows/Linux/Intel 构建。

## 四、工作分解

依赖关系：`S0 → (S1 ∥ S2 ∥ S3) → S4`。建议顺序 S1 → S2 → S3（S1 的渲染层两项极小，先拿到可见成果；S2 复用 2.0.0 管线，规格确定性高；S3 涉及滚动行为打磨，留最长联调时间）。

### S0 · 现状盘点与规格冻结（约 3 天）

产出统一落 `outputs/S0盘点_2.1.0_<日期>.md`，C0 评审通过后冻结范围。

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | CSS 排印属性支持度实测 | 在应用实际 Electron/Chromium 版本内实测四项：`text-autospace`、`text-spacing-trim`、`line-break: strict`、`hanging-punctuation`（`CSS.supports` + 渲染截图各一）。逐项给出「支持/不支持/部分」结论。**若 `text-autospace` 不支持，F1a 降级为本版仅导出 HTML 侧启用（交由目标浏览器渲染）并报 Lens 裁决** |
| 2 | 禁则现状实测 | 中文行首出现「，。」、行尾出现「「（」的复现文档实测当前引擎表现；结论写入报告（合格则关闭该项，不合格则记录差距供 Lens 决定是否排入后续） |
| 3 | 智能引号输入链路盘点 | 定位 Muya 输入管线中标点自动配对（`autoPairMarkdownSyntax` 等）的实现位置与 IME 守卫方式；确认插入弯引号对撤销栈、光标位置的影响路径。产出：F1c 的挂接点方案 |
| 4 | 一键排版的文本改写通道盘点 | 确认「对整篇/选区做程序化文本替换并合成单步撤销」的现有 API（Muya state 层批量操作 vs 逐段替换）；确认改写后光标/滚动位置保持策略。产出：F1d 执行通道方案 |
| 5 | 排印变量注入点复查 | 复查 2.0.0 双字体覆盖块的注入机制（编辑器 + 导出两处），确认字号/行高/段距/栏宽四变量以同一机制追加的可行性；盘点两套主题中这四项的当前取值与选择器落点 |
| 6 | 滚动与光标基建盘点 | 定位编辑器滚动容器、光标 viewport 定位 API、以及 2.0.0 缩略图视口同步用的滚动事件通道；确认打字机模式「保持光标行居中」可复用的既有能力；确认专注模式「当前活动块」信号源（Muya active block 类名） |
| 7 | 偏好与命令目录扩容点 | 新增偏好字段清单（见各特性规格）与命令目录新条目清单（专注模式、打字机模式、一键排版、排印面板入口），沿用 2.0.0 稳定命令 ID 体系 |
| 8 | 视觉稿 | F2 排印面板、F1d 一键排版确认对话框两处 UI 各出 ≥2 候选视觉稿，先给 Lens 确认再实现。F3 无新 UI（菜单勾选项 + 渲染效果），以行为规格表代替视觉稿 |

### S1 · F1 中文排印一期（约 1.5–2 周）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | F1a/F1b 渲染层启用 | 编辑器容器与导出主题（HTML/PDF/PNG 共用路径）启用 `text-autospace` 与 `text-spacing-trim`（具体值按 S0.1 实测定，倾向 `text-autospace: normal` + `text-spacing-trim: space-first`——终值 C0 冻结）。偏好新增总开关 `cjkTypographyEnabled`（默认开启，C0 确认），关闭时两属性同时移除。两套内置主题与 Typora 导入主题（转译器产物）均生效 |
| 2 | F1c 智能引号 | 按 S0.3 挂接点实现：直排输入 `"`/`'` 时，若判定为中文语境（规格 F1-05）则插入成对弯引号并置光标于中间；IME 组合态不触发；撤销一步还原为直引号输入前状态。偏好开关 `smartCjkQuotes`（默认关闭，C0 确认——避免打扰惯用直引号写代码/英文的用户） |
| 3 | F1d 一键排版命令 | 新命令「中文排版整理」（命令面板 + 格式菜单）：对全文或当前选区执行规格 F1-07 规则集；执行前弹确认对话框（视觉稿见 S0.8），列出各规则复选框与将改动的计数预估；执行为单步撤销；代码块/行内代码/公式/链接 URL/YAML front matter 一律豁免 |
| 4 | 导出一致性 | HTML/PDF/PNG 导出验证 F1a/F1b 生效；DOCX 不适用（pandoc 路线）并确认导出设置无误导文案 |
| 5 | 测试 | 单元：语境判定函数、排版规则集（每规则正反例 + 豁免区块全覆盖）。e2e：开关生效与持久化、智能引号输入（含 IME 守卫用例入矩阵）、一键排版执行与单步撤销、导出产物含排印属性 |

### S2 · F2 排印设置面板（约 1 周）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | 偏好字段 | 四字段：`readingFontSize`、`readingLineHeight`、`readingParagraphSpacing`、`readingColumnWidth`，各含哨兵值 `'theme'`（默认，取当前主题值）。三层加固写入 |
| 2 | 注入实现 | 复用 2.0.0 字体覆盖块机制，将四项合成入同一 `:root` 覆盖块（编辑器 + 导出双注入点）；「恢复主题默认」一键清除全部覆盖（含 2.0.0 双字体？——**否**，字体与排印分组独立重置，见规格 F2-04） |
| 3 | 面板 UI | 按 Lens 确认的视觉稿实现，落位偏好设置内与双字体同区（组成完整的「排印」分区）：字号步进器（12–24px）、行高（1.3–2.2，步进 0.05）、段距（0–2em）、栏宽（600–1200px 或「适应窗口」）；每项调整实时预览生效 |
| 4 | 测试 | 单元：合成器扩展分支（四项 × theme/自定义）。e2e：调整即时生效、重启保持、恢复默认回到主题快照、导出 HTML 含覆盖值、与 2.0.0 双字体设置互不干扰 |

### S3 · F3 专注与打字机模式（约 1–1.5 周）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | 专注模式 | 视图菜单 +命令面板新增「专注模式」：非活动块降不透明度（值按主题变量定，深浅色下均可读）；活动块信号复用 Muya active 类名；切换有 ≤200ms 淡入过渡；与划选工具条、表格工具条、缩略图共存无冲突 |
| 2 | 打字机模式 | 视图菜单 + 命令面板新增「打字机模式」：光标行保持视口垂直中线；子开关「仅输入时居中」（默认开启，对齐 Typora 细粒度）：开启时自由滚动不被拉回，仅键入字符时回中。IME 组合态期间不触发回中滚动（吸取 Lettera 打字机选区 bug 的教训，组合态结束后一次性对齐） |
| 3 | 状态与互斥 | 两模式独立开关、可同时开；均持久化；源码模式下两模式暂停生效（切回恢复）；模式开启状态在视图菜单打勾 |
| 4 | 测试 | e2e：开关与持久化、专注淡出类应用正确（活动块切换跟随）、打字机回中（含「仅输入时居中」两种行为差异断言）、IME 组合期间视口不跳、10k 行文档下开启两模式的性能门禁复测 |

### S4 · 发布工程（约 3 天）

| # | 任务 | 内容与产出 |
| --- | --- | --- |
| 1 | 验收遗留合入 | 分支 `claude/floralmd-reversion-comparison-jfanpi`（悬空括号修复 + 括号配平契约测试 + 两份调研/规划文档）合入 main |
| 2 | 版本与产物 | 2.1.0 版本号两处、DMG/ZIP/latest-mac.yml/sha256，归档 `releases/2.1.0/` |
| 3 | 发布说明与 README | 中文发布说明（更新弹窗渲染友好）；双语 README 更新（核心功能区新增「中文排印」条目——这是定位级卖点，不只是更新日志） |
| 4 | 更新链路回验 | 2.0.0 → 2.1.0 真机更新流全程；回退验证（新偏好字段容忍性） |

## 五、设计规格（验收基准，逐条可测）

### F1 中文排印（规格表）

| 编号 | 规格 |
| --- | --- |
| F1-01 | F1a/F1b 为纯渲染层效果：开启/关闭任意切换，文档源文件字节不变（e2e 断言保存后文件 hash 不变） |
| F1-02 | 总开关关闭时，渲染与 2.0.0 基线视觉一致（回归快照） |
| F1-03 | F1a 在编辑器、HTML、PDF、PNG 四处生效且视觉一致；Typora 导入主题的转译产物同样生效 |
| F1-04 | F1a/F1b 开启不影响光标命中与选区（在中西文混排行上点击落点、双击选词、方向键移动均与关闭时行为一致） |
| F1-05 | F1c 中文语境判定：光标前最近的非空白字符为 CJK 字符或 CJK 标点 → 中文语境；文档开头/行首默认非中文语境；行内代码、代码块、公式、链接 URL 内永不触发 |
| F1-06 | F1c 插入 `“”` 后光标位于两引号之间；紧接着输入 `"` 不再次配对（防连击）；Backspace 一次删除整对（若中间为空）；单步撤销回到输入前 |
| F1-07 | F1d 规则集 v1（每条独立复选，默认全选）：① CJK 与拉丁/数字之间补一个半角空格（豁免：标点相邻处）；② 中文语境下的半角 `,.!?:;()` 转全角（豁免：数字千分位/小数点/版本号模式）；③ 连续两个以上空格收敛为一个（豁免：行首缩进与 Markdown 语法性空格，如列表标记后、表格分隔）；④ 全角空格转半角空格 |
| F1-08 | F1d 豁免区块（代码块、行内代码、数学公式、链接/图片 URL 部分、YAML front matter、HTML 块）内容零改动；执行后为单步撤销；执行前对话框展示各规则将产生的替换计数 |
| F1-09 | F1d 对 10k 行文档执行 ≤ 2s，执行期间 UI 不冻结（分块处理或一次性状态替换按 S0.4 结论定） |
| F1-10 | F1c 在 IME 组合态（含拼音串中的引号候选）不触发；智能引号关闭时输入行为与 2.0.0 逐字节一致 |

### F2 排印面板（规格表）

| 编号 | 规格 |
| --- | --- |
| F2-01 | 四项默认值均为 `'theme'`，此时渲染与 2.0.0 基线逐像素一致；切换主题时跟随新主题值 |
| F2-02 | 任一项用户覆盖后：编辑器 ≤200ms 生效、不重载文档、不丢光标与滚动位置；导出 HTML/PDF/PNG 使用同一覆盖值 |
| F2-03 | 栏宽设置与侧栏/缩略图布局兼容：改栏宽不引发横向溢出，窄窗口下栏宽自动让位（min 与窗口约束按视觉稿定） |
| F2-04 | 「恢复主题默认」按钮只重置本分区四项，不触碰 2.0.0 双字体设置；字体区的既有重置也不触碰本四项 |
| F2-05 | 数值边界（12–24px / 1.3–2.2 / 0–2em / 600–1200px）在 UI 与偏好写入两层同时钳制；手改偏好文件越界值启动时回落边界值且不崩溃 |
| F2-06 | 行高/段距对代码块、表格、公式块不产生破坏性叠加（这三类块保持主题自身行高，规格上明确豁免选择器） |

### F3 专注与打字机（规格表）

| 编号 | 规格 |
| --- | --- |
| F3-01 | 专注模式：非活动块不透明度降至主题定义值（深浅色下正文仍可辨识，对比度 ≥ 3:1）；活动块及其内联工具（划选工具条）不受影响 |
| F3-02 | 活动块切换（点击/方向键/翻页）淡出跟随延迟 ≤200ms，无闪烁；图片/图表/表格块同样参与淡出 |
| F3-03 | 打字机模式（子开关关）：任何光标移动后光标行回到视口垂直中线（±0.5 行容差）；文档首尾不足半屏时不产生空滚动区之外的异常（顶部/底部允许自然贴边或补白按 C0 定，二选一冻结） |
| F3-04 | 打字机模式（「仅输入时居中」开，默认）：滚轮/拖动/翻页自由滚动不回拉；键入可见字符或回车后回中；Backspace 删除同回中 |
| F3-05 | IME 组合态期间不执行回中滚动；`compositionend` 后一次性对齐；组合期间视口无可感知跳动（e2e 断言组合期间 scrollTop 变化为 0） |
| F3-06 | 两模式与源码模式互斥暂停：进入源码模式效果暂停、菜单项置灰但状态保留，切回即恢复 |
| F3-07 | 两模式开启下 10k 行性能门禁不回退（P95 < 50ms）；专注淡出用 CSS 类切换实现，禁止逐块 JS 样式写入 |
| F3-08 | 命令目录新增条目沿用稳定 ID 体系（如 `view.focusMode`、`view.typewriterMode`、`format.cjkTidy`、`preferences.typography`），双语名齐备，进 2.0.0 面板可检索 |

## 六、节点与验收标准

评审物沿用既有模式：diff/PR + 测试证据 + 报告落 `outputs/`。评审人：Lens（终裁）+ Claude 会话（diff 级审查）。节点不过不进入下一阶段。

| 节点 | 时机 | 验收标准（全部满足才过） |
| --- | --- | --- |
| **C0 盘点与规格冻结** | S0 完成 | ① 盘点报告 8 项齐全、CSS 四属性支持度有截图证据；② 待决项已拍板：F1a/F1b 属性终值与总开关默认值、F1c 默认关、F3-03 首尾行为、F1d 规则集措辞；③ 两处视觉稿获 Lens 确认；④ 若 `text-autospace` 不支持，降级方案已裁决 |
| **C1 F1 验收** | S1 完成 | ① 规格 F1-01～F1-10 逐条过；② 规则集单元测试全分支；③ IME 新用例入矩阵并绿；④ 导出四路一致性截图对比通过；⑤ 源文件零改动断言（F1a/F1b）通过 |
| **C2 F2 验收** | S2 完成 | ① 规格 F2-01～F2-06 逐条过；② 默认值下与 2.0.0 逐像素一致；③ 边界钳制与损坏偏好容忍实测通过 |
| **C3 F3 验收** | S3 完成 | ① 规格 F3-01～F3-08 逐条过；② 10k 行 + 双模式开启性能复测达标；③ IME 组合态视口稳定 e2e 绿；④ 真机手工过一遍：全拼整句输入下打字机回中无跳动 |
| **C4 发布门禁** | S4 完成 | 第八节检查清单逐项过 → Lens 批准发布 |

## 七、移交切分：Codex 与保留项

**建议 Codex 承接**：S0.1–S0.7 盘点；S1、S2、S3 全部实现与测试；S4.2、S4.3 产物准备。

**保留给 Lens + Claude 会话**：C0–C4 节点评审与终裁；S0.8 视觉稿与四个待决项拍板；S4.1 分支合入、S4.4 真机回验与发布操作；F1d 规则集的中文措辞审校（对话框文案直接面向写作者，需 Lens 过目）。

**协作协议**（沿用既有模式）：任务按简报执行，DoD = 简报验收项全过 + 测试绿 + diff 审查无阻塞；Codex 产出以 diff/PR + 测试证据交付，不直接推送；`upstream/marktext` 分支操作限定 `reversion/2.1`；执行报告写入 `outputs/`（gitignored）。

**工期概览**：S0 约 3 天 → S1/S2/S3 合计约 3.5–4.5 周 → S4 约 3 天，总计 **4.5–5.5 周**。压缩优先级：先砍加分项，再允许 F1c 智能引号顺延一版（F1a/F1b/F1d 是定位核心，不砍）。

## 八、发布检查清单（正式版门禁）

1. C0–C3 节点全部通过，无未裁决项。
2. 全量测试绿：工具仓契约（≥100 项，含新括号配平测试）+ 应用仓单元 + e2e 冒烟/品牌 + muya 用例 + IME 矩阵（含 F1c/F3 新用例）。
3. 10k 行性能门禁（缩略图 + 专注 + 打字机全开状态）复测通过。
4. 2.0.0 验收遗留分支已合入 main。
5. 更新链路回验 + 回退验证真机通过。
6. 版本号与产物齐备一致，归档 `releases/2.1.0/`。
7. 正式版发布，严禁 pre-release；发布后回验 latest-mac.yml 可达、2.0.0 真机收到更新提示。
8. 双语 README（核心功能新增中文排印条目）、开发规划版本映射表、记忆文件同步更新。

## 九、执行者速查（Codex 开工必读）

**仓库结构**：本仓（工具仓）存主题/脚本/测试/发布管线；应用源码在 `upstream/marktext`（gitignored 独立仓，不得 push）；本里程碑工作分支 `reversion/2.1`（从 2.0.0 发布线切出）。本文档涉及上游源码的描述均为盘点前假设，**以 S0 实测为准**。

**常用命令**

```bash
npm test                      # 工具仓契约测试（现 100 项）
npm run test:e2e              # 应用级 e2e 冒烟 + 品牌断言
npm run test:muya-e2e         # 引擎自带用例（Chromium）
REVERSION_IME_ENGINE=muya2 npm run test:e2e:ime   # IME 矩阵
bash scripts/build-release-from-source.sh 2.1.0    # 源码直出发布产物
```

**纪律红线**

1. 只构建 arm64；不做 Apple 公证。
2. 不擅自扩大范围：3.3 节范围外事项只记录不动手；加分项未经 Lens 批准不得开工。
3. F1a/F1b 是渲染层特性，**任何改写用户源文件的行为都算缺陷**；F1d 是唯一允许改源文件的路径且必须单步撤销 + 豁免区块零改动。
4. 排印四项与双字体全部为 `'theme'` 默认值时，产物与 2.0.0 视觉逐像素一致是硬要求。
5. 新增命令走 2.0.0 稳定命令 ID 体系，双语名齐备，已发布 ID 不得改名。
6. IME 行为基准对齐 Typora 1.13.8；F1c 与 F3 的组合态守卫用例必须入矩阵。
7. 性能门禁在「缩略图 + 专注 + 打字机全开」最重状态下复测。
8. 面向用户界面文本无 MarkText 残留；新文案覆盖 10 种界面语言。
9. 任何发布操作仅由 Lens 执行。

## 十、后续版本候选（不属于本版承诺）

- **2.2.0 候选主轴**：主题明暗双配色一体化（学 Bear/Ghostwriter，根治深色模式短板）+ 复制到微信公众号。
- **观察项**：Lettera 正式版发布与定价（当前仍 beta、无主题系统、无移动端——反文的窗口期）；MarkText 上游 2026 年恢复维护后的 IME/光标修复回合排查；悬浮便签 + 全局快速记录；Footnotes 侧栏；句子级专注（iA Writer）。
