# Reversion 1.3.1

1.3.0 引擎迁移后的修订版，修复三处界面与首次启动问题，并重做行内格式工具条。建议所有 1.3.0 用户升级。

## 界面

- 修复宽窗口下编辑区右侧出现大片空白、滚动条停在窗口中间的问题。两套内置主题误将正文栏宽度限制加在了滚动视口上，现改为只约束居中的正文栏，编辑区始终占满窗口。
- 重做选中文字后弹出的行内格式工具条：圆角与描边收敛、按钮间距与悬停区域重排、当前已应用的格式以主题色淡底标示，清除格式独立成组。配色取自当前主题，各主题自动适配。

## 首次启动

- 全新安装不再弹出「更新说明」窗口。此前首次启动会把「没有历史版本记录」误判为「刚刚完成更新」。从旧版本升级时仍会在安装后显示一次本次更新说明。
- 系统处于深色外观时，全新安装不再被自动切换为 dark 主题，默认主题恢复为 Lens Design。如需跟随系统外观切换主题，可在「偏好设置 → 通用」中开启，浅色 / 深色对应主题可分别指定。

## 工程

- 修复长文档 PDF 相关单元测试在并行执行下的偶发失败。
- 清理 1.3.0 遗留的 ESLint 错误。

发布门禁：工具仓契约测试 95、Desktop 单元测试 777、桌面 e2e 20、Muya 2 输入法矩阵 72，全部通过。

## 兼容性

- Apple Silicon（arm64）
- macOS 应用路径：`/Applications/Reversion.app`
- 用户数据目录继续使用 `~/Library/Application Support/marktext`
- Bundle ID 继续使用 `com.github.marktext.marktext`
- 可从 1.2.0 / 1.3.0 通过应用内更新直接升级

反文采用稳定 ad-hoc 签名，未经 Apple 公证。首次打开如被 Gatekeeper 阻止，请在访达中按住 Control 点击应用，再选择「打开」。
