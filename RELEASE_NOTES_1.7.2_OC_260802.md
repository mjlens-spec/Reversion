# Reversion 1.7.2

本次版本集中完善 1.7.0 工作区改版后的侧栏对齐、底部信息区、常驻标签栏与右上角导出入口。

## 改进

**侧栏入口居中。**「文件 / 搜索 / 目录」按钮内的图标与色块重新对齐，当前入口继续显示图标与文字，其余入口保持图标优先。

**底部工具区恢复。** 设置入口回到侧栏左下角；右下角恢复实时中文与英文计数，悬停或聚焦后可查看完整字数、字符数和段落数。

**设置符号更新。** 原有齿轮替换为圆角线性调节图标，视觉重量更轻，并保持原有设置窗口入口与键盘焦点反馈。

**导出入口改为纯图标。** 右上角以「箭头离开托盘」符号配合紧凑的下拉箭头表达导出。闲置时不显示文字、底色、边框或投影；悬停、键盘聚焦或菜单展开时才显示轻量状态反馈。HTML、PDF、DOCX 与 PNG 四种导出路径保持不变。

**标签栏在窄窗口和多标签下保持可用。** 标签列表在工作区可用宽度内滚动，新建标签的「+」保持紧邻标签且不会被挤出；从菜单或快捷操作切换到溢出区域的标签时，会自动把当前标签滚回视野。

## 验收

- Desktop 单元测试：819/819
- Electron 源码定向回归：21/21
- 正式打包应用 E2E：20/20
- 工具仓契约测试：98/98
- 35 个内置主题的导出图标对比度：全部不低于 3:1，最低为 Solarized Light 的 3.244:1
- TypeScript、ESLint、production build：通过
- 浅色与暗色视觉截图：Idle / Hover / Open 六种状态通过
- Sol 5.6 X-High 工程与视觉终审：GO

## 兼容性

- Apple Silicon（arm64）
- macOS 应用路径：`/Applications/Reversion.app`
- 用户数据目录继续使用 `~/Library/Application Support/marktext`
- Bundle ID 继续使用 `com.github.marktext.marktext`
- 反文采用稳定 ad-hoc 签名，未经 Apple 公证
